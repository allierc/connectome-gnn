# Opto training status — 2026-05-11

Source: scan of `/groups/saalfeld/home/allierc/GraphData/log/fly/flyvis_noise_free_blank50_opto_*/tmp_training/metrics.log`
Target: **1.6M iterations** (`tmp_training/total_iter.txt` = 1600000 — last logged step is iter 1,520,001, written every 80k)
Sentinels: `_complete` = training done, `_cross_test_plot_complete` = test+plot done.

## Headline counts (over 195 opto log dirs)

| status | count |
|---|---|
| ✅ trained + tested + plotted | **140 / 195** |
| ⚠️ trained (≥1.52M iter) — test+plot pending | **34 / 195** |
| 🟡 training in-progress / stopped (480k iter) | **21 / 195** |
| ⛔ dataset exists but no log dir yet | **10** (Tm3_05 × 5, L5_dc_05_cv01..04 × 4 — L5_dc_05_cv00 also missing) |

The 21 stopped runs you killed this morning match the 21 LSF jobs (`*ain_00..20.sh`) — all submitted **May 10 21:08** queue / started **May 11 00:08** runtime / killed **~05:00 May 11** at iter ≈480,001.

## Test+plot still owed (34 folds — all `dc_05` waveform)

| condition | trained folds | tested folds | note |
|---|---|---|---|
| `Mi1_dc_05`         | 5/5 | 0/5 | test+plot pending |
| `Mi4_dc_05`         | 5/5 | 0/5 | test+plot pending |
| `Tm1_dc_05`         | 5/5 | 0/5 | test+plot pending |
| `Tm4_dc_05`         | 5/5 | 0/5 | test+plot pending |
| `TmY15_dc_05`       | 5/5 | 0/5 | test+plot pending |
| `TmY15+Mi1_dc_05`   | 5/5 | 0/5 | test+plot pending |
| `retina_dc_05`      | 4/5 | 0/5 | cv04 still at 480k (stopped) |
| `L4_dc_05`          | 0/5 | 0/5 | all 5 folds stopped at 480k |

→ Run `python run_GNN_optogenetics.py --replot` to dispatch the test+plot wave for the trained `dc_05` folds.

## Training in-progress / stopped — restart needed

| condition | folds at iter ≈480k | dataset complete? |
|---|---|---|
| `L4_dc_05`          | cv00..04 (5) | yes |
| `Lawf2_05`          | cv00..04 (5) | yes |
| `Lawf2_heaviside`   | cv00..04 (5) | yes |
| `Lawf2_heaviside_05`| cv00..04 (5) | yes |
| `retina_dc_05_cv04` | cv04 (1)     | yes |

→ Total: 21 partial folds. Re-running `python run_GNN_optogenetics.py` will pick these up (the script's `_is_trained` check looks for `best_model_with_*.pt`, which exists for these partial runs from intermediate saves — you may need `--retrain` or wipe `models/` to force a fresh start).

## Datasets ready but not yet submitted to train

- `Tm3_05_cv00..04` (5 folds — no log dir at all yet)
- `L5_dc_05_cv01..04` (4 folds — dataset not generated; only cv00 exists)

→ Run `run_generate_optogenetics.py` first to fill L5_dc_05 missing folds, then `run_GNN_optogenetics.py` to train Tm3_05.

## Fully trained & tested (140 folds, 28 conditions × 5 folds)

`TmY15_{05,heaviside,heaviside_05}`, `Mi1_{05,heaviside,heaviside_05}`, `Tm4_{05,heaviside,heaviside_05}`,
`Tm1_{05,heaviside,heaviside_05}`, `Mi4_{05,heaviside,heaviside_05}`, `TmY15+Mi1_{05,heaviside,heaviside_05}`,
`retina_{05,heaviside,heaviside_05}`, `retina_heaviside_var`,
`L4_{05,heaviside,heaviside_05}`, `L5_{05,heaviside,heaviside_05}`.

R² metrics for these are tabulated in [../log/remote/cv_blank50_opto_summary.md](../log/remote/cv_blank50_opto_summary.md) (auto-regenerated by `run_GNN_optogenetics.py`).

## On the "400k/1.6M after 1–2 days" puzzle

Looking at the LSF jobids you listed:
- `SUBMIT_TIME = May 11 00:08` → they ran for ~5 h before you killed them at iter ≈480k. That's the normal training rate (~80k iter/h on gpu_l4 → 1.6M would take ~20 h).
- `cluster_train_NN.sh` files on disk are stamped **May 10 21:08** — that's the bsub-time, with ~3 h LSF queue wait before the job actually started running on May 11 00:08.
- So these jobs were submitted only ~10 h before you stopped them, not 1–2 days. The "I started these 1–2 days ago" memory is likely from an earlier wave for *other* conditions (the 140 fully-trained ones in `/log/fly/`). Each wave of `run_GNN_optogenetics.py` re-screens which folds still need training and submits only the missing ones, so the dc_05 + Lawf2 wave is genuinely fresh.

No anomaly — just a queue-wait illusion plus a long target (1.6M iter ≈ 20 h on this hardware).
