% Standalone TikZ figure — output: architectures.pdf
% Intended to be included in a NeurIPS paper via \includegraphics{architectures.pdf}.
% The caption lives in the main paper; see architectures_caption.tex.
\documentclass[tikz,border=2pt]{standalone}
\usepackage{amsmath,amssymb}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc,fit,backgrounds,shapes.geometric}

\begin{document}

\tikzset{
  every node/.style={font=\small},
  inp/.style={draw,rounded corners=1pt,fill=gray!10,minimum height=6mm,minimum width=12mm,inner sep=2pt,align=center},
  mlp/.style={draw,rounded corners=2pt,fill=orange!15,minimum height=8mm,minimum width=16mm,align=center,inner sep=3pt},
  outp/.style={draw,rounded corners=1pt,fill=green!15,minimum height=6mm,minimum width=12mm,align=center,inner sep=2pt},
  op/.style={draw,circle,inner sep=0pt,minimum size=5mm,fill=white},
  flow/.style={-{Stealth[length=2mm]},thick},
  oplbl/.style={font=\scriptsize\itshape,gray!60!black},
  title/.style={font=\bfseries},
}

\begin{tikzpicture}[node distance=6mm and 8mm]

% ==================== (B) MLP baseline ====================
\begin{scope}[local bounding box=A,shift={(8.5,0)}]
  \node[title] (tA) at (0,0) {(B) MLP baseline};
  \node[inp,below=14mm of tA.west,anchor=west] (vA) {$\mathbf{v}(t)$};
  \node[inp,below=8mm of vA]                  (sA) {$\mathbf{s}(t)$};
  \node[op,right=10mm of $(vA)!0.5!(sA)$]     (catA) {$\oplus$};
  \node[oplbl,left=0.8mm of catA]             {concat};
  \node[mlp,right=6mm of catA]                (mlpA) {MLP};
  \node[outp,right=6mm of mlpA]               (oA)   {$\dfrac{d\mathbf{v}}{dt}$};

  \draw[flow] (vA.east) -| (catA.north);
  \draw[flow] (sA.east) -| (catA.south);
  \draw[flow] (catA) -- (mlpA);
  \draw[flow] (mlpA) -- (oA);
\end{scope}

% ==================== (C) EED ====================
\begin{scope}[local bounding box=B,shift={(17,0)}]
  \node[title] (tB) at (0,0) {(C) Encode--Evolve--Decode (EED)};

  \node[inp,below=14mm of tB.west,anchor=west] (vB) {$\mathbf{v}(t)$};
  \node[mlp,right=4mm of vB]                  (encB){Encoder};
  \node[op,right=4mm of encB]                 (zB)  {$\mathbf{z}$};

  \node[inp,below=14mm of vB]                 (sB)  {$\mathbf{s}(t)$};
  \node[mlp,right=4mm of sB]                  (sencB){Stim.\ Enc.};
  \node[op,right=4mm of sencB]                (sLB) {$\mathbf{s}_z$};

  \node[op,right=4mm of zB]                   (catB){$\oplus$};
  \node[oplbl,anchor=north east]              at ($(catB.south west)+(1mm,-0.3mm)$) {concat};
  \node[mlp,right=4mm of catB]                (evoB){Evolver};
  \node[op,right=4mm of evoB]                 (addB){$+$};
  \node[oplbl,below=0.5mm of addB]            {add};
  \node[mlp,right=4mm of addB]                (decB){Decoder};
  \node[outp,right=4mm of decB]               (vnB) {$\mathbf{v}(t{+}\Delta t)$};

  \draw[flow] (vB) -- (encB);
  \draw[flow] (encB) -- (zB);
  \draw[flow] (sB) -- (sencB);
  \draw[flow] (sencB) -- (sLB);
  \draw[flow] (zB) -- (catB);
  \draw[flow] (sLB) -| (catB);
  \draw[flow] (catB) -- (evoB);
  \draw[flow] (evoB) -- (addB);
  \draw[flow] (zB.north) to[out=30,in=150] (addB.north);
  \draw[flow] (addB) -- (decB);
  \draw[flow] (decB) -- (vnB);
\end{scope}

% ==================== (A) Stimulus baseline ====================
\begin{scope}[local bounding box=C]
  \node[title] (tC) at (0,0) {(A) Stimulus-only baseline};

  \node[inp,below=14mm of tC.west,anchor=west]            (s1) {$\mathbf{s}(t{-}t_w{+}1)$};
  \node[inp,below=2mm of s1.south east,anchor=north east] (s2) {$\vdots$};
  \node[inp,below=2mm of s2.south east,anchor=north east] (s3) {$\mathbf{s}(t)$};

  \node[mlp,anchor=west] (e1) at ($(s1.east)+(5mm,0)$) {Stim.\ Enc.};
  \node[mlp,anchor=east] (e3) at (e1.east |- s3)       {Stim.\ Enc.};

  \node[op,right=10mm of $(e1)!0.5!(e3)$]     (catC){$\oplus$};
  \node[oplbl,left=0.8mm of catC]             {concat};
  \node[mlp,right=6mm of catC]                (pred){Predictor};
  \node[outp,right=6mm of pred]               (oC)  {$\hat{\mathbf{v}}(t)$};

  \draw[flow] (s1) -- (e1);
  \draw[flow] (s3) -- (e3);
  \draw[flow] (e1.east) -| (catC.north);
  \draw[flow] (e3.east) -| (catC.south);
  \draw[flow] (catC) -- (pred);
  \draw[flow] (pred) -- (oC);
\end{scope}

\end{tikzpicture}
\end{document}
