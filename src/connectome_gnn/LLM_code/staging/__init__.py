"""Staging root — per-block subpackages (block_01/, block_02/, ...) hold the
mechanism functions and analyses the agent writes during Phase S and Phase R.
Production code only references these; never moves them inline.
"""
