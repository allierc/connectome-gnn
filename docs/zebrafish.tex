\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{natbib}
\usepackage{geometry}
\geometry{margin=2.5cm}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{float}
\usepackage[table]{xcolor}
\usepackage{subcaption}
\usepackage{cleveref}
\crefname{equation}{Eq.\@}{Eqs.\@}
\Crefname{equation}{Eq.\@}{Eqs.\@}
\crefname{figure}{Fig.\@}{Figs.\@}
\Crefname{figure}{Fig.\@}{Figs.\@}
\crefname{table}{Tab.\@}{Tabs.\@}
\Crefname{table}{Tab.\@}{Tabs.\@}
\crefname{section}{Sect.\@}{Sects.\@}
\Crefname{section}{Sect.\@}{Sects.\@}

\definecolor{cgreen}{HTML}{2ea043}
\definecolor{corange}{HTML}{d29922}
\definecolor{cred}{HTML}{cf222e}
\newcommand{\good}[1]{\textcolor{green!50!black}{#1}}
\newcommand{\bad}[1]{\textcolor{orange}{#1}}

\def\eg{{\it e.g.}}
\def\ie{{\it i.e.}}
\def\R{\mathbb{R}}
\def\relu{\mathrm{ReLU}}
\def\softplus{\mathrm{softplus}}
\def\petrucco{Petrucco et al.\ 2023}
\def\rpi{r1\ensuremath{\pi}}
% Functional-class macros (Results section)
\newcommand{\mi}{\mathcal{I}}
\newcommand{\omag}{\mathcal{W}}
\newcommand{\smod}{\mathcal{S}}
\newcommand{\specf}{\mathcal{T}}
\newcommand{\cls}[1]{\mathsf{#1}}

% Figure directory. Change ONLY this line when moving between editors:
%   Overleaf (main.tex next to figures/):  \newcommand{\figdir}{figures/zebrafish}
%   local repo (docs/ build):              \newcommand{\figdir}{../figures/zebrafish}
\newcommand{\figdir}{../figures/zebrafish}

% No first-line paragraph indent; add a small inter-paragraph skip so the
% paragraph boundary stays visually obvious.
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.5em plus 0.1em minus 0.05em}

\title{\textbf{Zebrafish anterior hindbrain / dIPN: integrating heading
direction, forward velocity, and 2-D position from a single
connectome-locked circuit}}
\author{}
\date{}

\begin{document}
\maketitle
\begin{abstract}
We study a vertebrate self-motion integrator at single-neuron
resolution: the GABAergic \rpi{} population of the larval-zebrafish
anterior hindbrain (\citealp{petrucco2023}), together with the three
afferent populations the \emph{fish2} inventory identifies as carrying
its angular and translational drives. From the \emph{neuprint-fish2}
EM reconstruction we build sign-locked RNN and message-passing GNN
models whose connectivity is fixed to the connectome under an
all-inhibitory Dale convention, and train the same circuit on three
progressively harder integration sub-tasks: heading, heading $+$
forward distance, and 2-D path integration with a dual exteroceptive
$+$ proprioceptive translation afferent. Optimising the
input--output map alone recovers a sharp heading code, but the
preferred-heading map is not anatomically organised and many
connectome-consistent circuits integrate equally well, so the
implementation is degenerate. The task-only model's population
activity is also far from the measured ZAPBench calcium; adding a
calcium-observation loss raises the predicted-vs-recorded per-neuron
correlation from $\rho \approx 0.06$ to $0.4$--$0.55$ while leaving
heading accurately decoded.
\end{abstract}

\section{Introduction}

In the larval zebrafish, head-direction information is carried by a
population of hundreds of GABAergic inhibitory neurons (the \rpi{} cells)
located in rhombomere~1 of the anterior hindbrain \citep{petrucco2023}.
A sinusoidal ring activity peak travels around the functional ring
(cells ordered by preferred heading, not by anatomical position) in
a direction opposite to the fish's swims, persists for tens of
seconds in the absence of motor activity, and tracks an estimated
heading angle whose magnitude per swim ($\approx \pi/4$~rad) matches
the behaviourally measured turn. The connectivity scheme that
supports the ring activity is the classical ring-attractor
architecture. The electron-microscopy reconstructions of the \rpi{}
axons reveal reciprocal connectivity in the dIPN consistent with
this picture.

\section{Anatomy, functions and connectome}
\label{sec:anatomy}

\paragraph{3-D anatomy.}
\Cref{fig:zhd_3d} shows the circuit cell families rendered in
3-D and colour-coded by the same seven-way functional partition used
in the circuit diagram: the recurrent ring substrate
(\emph{dIPN} $=$ \emph{IPNd*} $+$ \emph{IPNds*}), the \emph{IPN12}
pool, and the \emph{IPN-core} families \emph{IPN28/29/31--36}; the
three afferent populations that
the velocity gate routes the angular and translational drives through
(\emph{ARTR} $=$ \emph{RIPN01/02/03\_a/03\_b}, \emph{pt-IPN1}, and
\emph{motor\_efferent} $=$ \emph{RIPN11/12\_a/12\_c}); and a residual
``other'' category for sub-types not routed through the gate
(\emph{RIPN05/12\_b}). Spatially, the
\emph{dIPN} substrate forms the compact midline neuropil;
\emph{RIPN}-derived afferents descend from the habenula (a paired
diencephalic nucleus relaying limbic and modulatory signals to the
hindbrain) and \emph{pt-IPN}-derived afferents sweep in from the
lateral pretectum (a midbrain visual-processing region carrying optic-
flow and water-flow signals).

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_1_overview.png}
  \caption{\textbf{Zebrafish HD / IPN circuit --- anatomy
  (\textbf{a}, \textbf{b}) and afferent partition (\textbf{c},
  \textbf{d}).} Seven-way colour code shared across panels:
  \emph{dIPN} (amber, $n=408$; ring substrate),
  \emph{IPN12} (rose, $n=108$; recurrent), \emph{IPN-core}
  (teal, $n=184$; recurrent),
  \emph{ARTR} (blue, $n=76$; angular afferent), \emph{pt-IPN1}
  (orange, $n=51$; exteroceptive translation), \emph{motor\_efferent}
  (green, $n=65$; angular/heading efference), \emph{other}
  (grey, $n=25$).
  \textbf{(a)}~Full \emph{neuprint-fish2} skeletons against Habenula /
  dIPN / vIPN ROI silhouettes; dorsal view.
  \textbf{(b)}~Same view restricted to cell bodies (SWC soma nodes).
  Panels (a, b) show spatially disjoint compartments of the same
  neurons: the dense central region of (a) is the dIPN / iIPN
  neuropil (no cell bodies), the somata sit in the surrounding cortex.
  \textbf{(c)}~Velocity-gate partition on the $917$-cell connectome:
  $\omega$ routed exclusively to ARTR and the exteroceptive forward
  channel $v_{\mathrm{ext}}$ routed exclusively to pt-IPN1; rotation
  and translation paths are anatomically separated.
  \textbf{(d)}~Proprioception extension: the motor-efferent RIPN
  sub-types (RIPN11/12\_a/12\_c) carry an efference copy of the
  \emph{heading/turn} command and are driven by a separate companion
  angular channel $\omega_{\mathrm{proprio}}$ in parallel with ARTR (green box
  between the ARTR and pt-IPN1 boxes), modelling sensory plus
  efference-copy redundancy on the \emph{angular} drive.
  Recurrent network and decoder are identical between (c) and (d);
  only the gate-target taxonomy differs.}
  \label{fig:zhd_3d}
\end{figure}

\paragraph{Functional circuit.}
\label{method:proprioception}
The angular (heading) drive reaches IPN through two complementary
pathways (\cref{fig:zhd_3d}\textbf{d}): the \emph{sensory} ARTR
afferents and a \emph{motor-efference} copy via the motor-efferent
RIPN sub-types \emph{RIPN11}, \emph{RIPN12\_a}, \emph{RIPN12\_c}
($36+29$ cells per side, glutamatergic) --- which the \emph{fish2}
inventory ties to head direction, not forward swim --- carrying the
turn command's efference copy. The proprioception variant adds these
cells as a companion \emph{angular} gate target: $\omega$ now drives
both \emph{ARTR} and \emph{motor\_efferent} in parallel (each with its
own pair of learnable scalars), while $v_{\mathrm{fwd}}$ routes only
to \emph{pt-IPN1}. This rotation/translation split of the afferent
partition (\cref{fig:zhd_3d}) mirrors the functional organisation
\citet{feierstein2023} report in the zebrafish hindbrain: applying
reduced-rank regression to whole-brain GCaMP6s imaging, they find that
behaviour-related population activity is low-dimensional --- captured by
two features --- and that the corresponding voxels segregate into a
vergent-movement/swimming (translational) cluster and two rotation
clusters that lie selectively in the left and right hemispheres.

\paragraph{Afferent cell types.}
The constituent afferent sub-types carry distinct, identifiable
signals. \emph{ARTR} --- the Anterior Rhombomere Turning Region --- is
a long-time-constant population that typically becomes active only
after the first turning event and then sustains its activity over a
relatively long window. It is built from two similar pairs.
\emph{RIPN01} and \emph{RIPN03\_a} share a connectivity pattern that
differs essentially only in their ring target: \emph{RIPN01} projects
to \emph{dIPN}, whereas \emph{RIPN03\_a} projects to \emph{dsIPN}
($=$\emph{IPNds*}), a small region just posterior to dIPN.
\emph{RIPN02} and \emph{RIPN03\_b} form a second, structurally similar
pair --- with the targets swapped (\emph{RIPN03\_b}$\to$\emph{dIPN},
\emph{RIPN02}$\to$\emph{dsIPN}) --- that reads out the \emph{hSAG}
pathway carrying horizontal rotational signals; these cells are not yet
reliably driven in the present data (their activity resembles noise),
but their connectivity places them with ARTR.
\emph{pt-IPN1} integrates optic-flow and water-flow signals and divides
into roughly four groups, each encoding a different direction.
The motor-efference cells split by efferent source: \emph{RIPN11}
receives \emph{ascending} motor efferents together with the \emph{hSAG}
pathway and is our leading candidate for moving the heading bump, while
\emph{RIPN12\_a} and \emph{RIPN12\_c} --- best treated as a single
group --- receive \emph{descending} motor efferents together with
\emph{hSAG}.

\paragraph{Connectome.}
\Cref{fig:zhd_connectome_summary} depicts the nominal $917$-cell
circuit, its binary support $\mathrm{supp}(W^{\mathrm{con}})$ and the
signed $W^{\mathrm{con}}$. The matrix is sparse (density $0.037$,
$30\,851$ non-zero edges out of $917^2 = 840\,889$); edge weights are
the \emph{fish2} synapse contact areas, spectrally normalised to
radius $0.9$. The recurrent substrate --- the \rpi{} cells
(\emph{IPNd*}, \emph{IPNds*}), the \emph{IPN12} pool, and the
\emph{IPN-core} families --- is taken GABAergic \citep{petrucco2023}: its
outgoing weights are sign-flipped to inhibitory, giving an
all-inhibitory recurrent substrate ($E\!:\!I = 10\,406\!:\!20\,445$,
$33.7\%$ / $66.3\%$). \emph{RIPN*} and \emph{pt-IPN*} afferents have
no Dale annotation on the \emph{fish2} server and are kept positive
(excitatory).

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_2_connectome.png}
  \caption{\textbf{Zebrafish HD / IPN connectome (nominal $917$-cell
  circuit).} Matrix size $N \times N = 917 \times 917$, $30{,}851$
  non-zero edges (density $=0.037$); edge weights are \emph{fish2}
  synapse contact areas, the recurrent ring Dale-flipped inhibitory so
  $E\!:\!I = 10{,}406\!:\!20{,}445$ ($33.7\%$ / $66.3\%$). Both panels
  share the row/column ordering: neurons are sorted by the seven-way
  functional partition introduced in \cref{fig:zhd_3d}
  --- \emph{dIPN} (amber, $n=408$), \emph{IPN12} (rose, $n=108$),
  \emph{IPN-core} (teal, $n=184$), \emph{other} (grey, $n=25$),
  \emph{ARTR} (blue, $n=76$), \emph{pt-IPN1} (orange, $n=51$),
  \emph{motor\_efferent} (green, $n=65$) --- so the same block appears
  at the same matrix coordinates across (a) and (b), and the colour
  code matches \cref{fig:zhd_3d}.
  \textbf{(a)} Support mask. Colour-coded strips on the row and column
  axes mark each partition block, and translucent column bands inside
  the matrix carry the same colour code.
  \textbf{(b)} Signed $W^{\mathrm{con}}$, $z$-scored (red excitatory,
  blue inhibitory).}
  \label{fig:zhd_connectome_summary}
\end{figure}

\section{Methods}
\label{sec:methods}

\textbf{Recurrent neural network.}\label{method:RNN}
The RNN is a discrete-time recurrent network for
$i\in\{1,\dots,N\}$ with $N=917$:
\begin{equation}
\tau\,\frac{d\hat h_i(t)}{dt}
  = -\hat h_i(t)
  + \sum_j \hat W^{\mathrm{rec}}_{ij}\,\sigma\!\big(\hat h_j(t)\big)
  + \sum_l \hat W^{\mathrm{in}}_{il}\,u_l(t)
  + \hat b_i,
\label{eq:zhd_dynamics}
\end{equation}
$\sigma$ is the sigmoid, $\tau=0.1\,\mathrm{s}$, $\Delta t=0.01\,\mathrm{s}$.
The recurrent matrix is sign-locked to the connectome template
$W^{\mathrm{con}}\in\R^{N\times N}$ (built from the
\emph{neuprint-fish2} reconstruction; outgoing columns of GABAergic
HD-ring cells \emph{IPNd*}, \emph{IPNds*} are negated, and the whole
matrix is spectrally normalised to radius $0.9$): the trainable
per-edge parameter is the magnitude $\hat S$, and
\begin{equation}
\hat W^{\mathrm{rec}}_{ij}
  = |\hat S_{ij}|\,\mathrm{sign}(W^{\mathrm{con}}_{ij}).
\label{eq:zhd_w_rec}
\end{equation}
\bigskip
\noindent\textbf{Input gate on $\hat W^{\mathrm{in}}$: anatomically licensed afferent ports.}%
\label{method:input_extended}
$\hat W^{\mathrm{in}}\in\R^{N\times n_{\mathrm{in}}}$ has one row per
neuron ($N=917$) and one column per input channel
($n_{\mathrm{in}}=\dim u(t)$). At every step it injects the external
drive $\hat W^{\mathrm{in}}u(t)\in\R^{N}$ into the dynamics, added to
the recurrent term $\hat W^{\mathrm{rec}}\sigma(\hat h)$ of
\cref{eq:zhd_dynamics}. The rule is that each input variable may touch
only the one afferent cell type that
\cref{fig:zhd_3d}\textbf{c}--\textbf{d} assigns to it; the three
afferent types are the only entry points, and the ring cells
(\emph{IPNd*}, \emph{IPNds*}, \emph{IPN12}) never receive external
input.

Take the exteroceptive variant of \cref{fig:zhd_3d}\textbf{c}, with the
four-channel drive
\begin{equation*}
u(t)=\bigl(\,\omega,\ v_{\mathrm{fwd}},\
   \cos\theta_0\!\cdot\!\delta_{t_0},\
   \sin\theta_0\!\cdot\!\delta_{t_0}\,\bigr)^{\!\top},\qquad
\omega\to\mathrm{ARTR},\quad v_{\mathrm{fwd}}\to\text{pt-IPN1}.
\end{equation*}
Grouping the $N$ rows by population, the signal injected into the
network at time $t$ --- the $+\hat W^{\mathrm{in}}u(t)$ term of
\cref{eq:zhd_dynamics} --- is the matrix--vector product
{\small
\begin{equation}
\begin{gathered}
\hat W^{\mathrm{in}}\,u(t)=
\bordermatrix{%
 & \omega & v_{\mathrm{fwd}} & \cos\theta_0 & \sin\theta_0 \cr
\mathrm{ARTR}_L   & -\softplus(\tilde v_\omega^{L}) & 0 & * & * \cr
\mathrm{ARTR}_R   & +\softplus(\tilde v_\omega^{R}) & 0 & * & * \cr
\text{pt-IPN1}_L  & 0 & v_{\mathrm{fwd}}^{L} & * & * \cr
\text{pt-IPN1}_R  & 0 & v_{\mathrm{fwd}}^{R} & * & * \cr
\text{ring}       & 0 & 0 & * & * \cr
\text{other aff.} & 0 & 0 & * & * \cr}
\begin{pmatrix}\omega\\[1pt] v_{\mathrm{fwd}}\\[1pt]
   \cos\theta_0\,\delta_{t_0}\\[1pt] \sin\theta_0\,\delta_{t_0}\end{pmatrix}
\\[6pt]
=\ \omega\,\mathbf{w}_\omega+v_{\mathrm{fwd}}\,\mathbf{w}_{v}
  +\delta_{t_0}\bigl(\cos\theta_0\,\mathbf{c}+\sin\theta_0\,\mathbf{s}\bigr)
  \ \in\R^{N},
\end{gathered}
\label{eq:zhd_swim_gate}
\end{equation}
}
where ``$*$'' marks a free (unconstrained) entry and
$\mathbf{w}_\omega,\mathbf{w}_{v},\mathbf{c},\mathbf{s}\in\R^{N}$ are
the four columns of $\hat W^{\mathrm{in}}$. The three column kinds are:
the \emph{sign-locked turn column} $\mathbf{w}_\omega$
($\omega\!\to\!$ARTR), nonzero only on ARTR with its left half forced
inhibitory and right half excitatory via $\mp\softplus$ --- the two
scalars $\tilde v_\omega^{L},\tilde v_\omega^{R}$ are the only learned
parameters, and the lock makes left/right turns drive the ring peak in
opposite directions (a same-sign gate would integrate heading equally
well but drive both hemispheres in phase, unlike the data); the
\emph{unlocked vigour column} $\mathbf{w}_{v}$
($v_{\mathrm{fwd}}\!\to\!$pt-IPN1), non-directional, keeping the pt-IPN1
restriction but dropping the lock (one free scalar per side, either
sign); and the \emph{free cue columns} $\mathbf{c},\mathbf{s}$
($\cos\theta_0,\sin\theta_0$), dense and fully free over all $N$ rows,
which $\delta_{t_0}$ switches on only at the first frame $t_0$ to plant
the starting heading bump into the ring before they fall silent.
The proprioception variant of \cref{fig:zhd_3d}\textbf{d} adds
a fifth channel,
\begin{equation*}
u(t)=\bigl(\,\omega,\ v_{\mathrm{fwd}},\ \omega_{\mathrm{proprio}},\
   \cos\theta_0\!\cdot\!\delta_{t_0},\
   \sin\theta_0\!\cdot\!\delta_{t_0}\,\bigr)^{\!\top},\qquad
\omega_{\mathrm{proprio}}\to\mathrm{motor\_efferent},
\end{equation*}
i.e.\ a \emph{second} sign-locked angular column routed to
motor\_efferent (RIPN11 + RIPN12\_a + RIPN12\_c, which the \emph{fish2}
inventory ties to head direction, carrying the turn command's efference
copy), while $v_{\mathrm{fwd}}$ still routes only to pt-IPN1. In the
first model version the two angular columns share a driver,
$\omega_{\mathrm{proprio}}(t)=\omega(t)$, each with its own pair of
scalars. The same rule scales to the full multi-modal driver of the
nine ZAPBench conditions (\cref{fig:zhd_allblocks}): every visual
channel (rotation/optic-flow velocity, whole-field luminance, L/R light
difference, random-dot motion) drives pt-IPN1, and every tail-EMG
self-motion channel drives RIPN, each gated by \cref{eq:zhd_swim_gate}.
Anatomy fixes \emph{which} port a modality uses, the sign-lock fixes its
L/R \emph{polarity}, and only the gain $\softplus(\tilde v_c^{L,R})$ is
free.

\bigskip
\noindent\textbf{Output readout on $\hat W^{\mathrm{out}}$: task-conditioned columns.}%
\label{method:output_readout}
$\hat W^{\mathrm{out}}$ reads from the HD ring (\emph{IPNd*},
\emph{IPNds*}, \emph{IPN12 pool}) and never from the afferent
populations. Its column dimension $n_{\mathrm{out}}$ is set by the
active sub-task (\cref{fig:zhd_swim_task_modes}):
\begin{equation}
\hat y(t) =
\begin{cases}
(\cos\hat\theta,\sin\hat\theta)                 & n_{\mathrm{out}}=2 \ \ (\text{heading only}),\\
(\cos\hat\theta,\sin\hat\theta,\hat d)          & n_{\mathrm{out}}=3 \ \ (\text{heading}+\text{forward distance}),\\
(\cos\hat\theta,\sin\hat\theta,\hat x,\hat y)   & n_{\mathrm{out}}=4 \ \ (\text{2D path integration}),
\end{cases}
\label{eq:zhd_readout}
\end{equation}
with $\hat y = \hat W^{\mathrm{out}}\,\sigma\!\big(\hat h(t)\big)+\hat b^{\mathrm{out}}$,
$\hat\theta=\operatorname{atan2}(\hat y_2,\hat y_1)$. The leaky-vs-cumulative choice
(\cref{eq:xi_leaky,eq:xy_pi}) changes only what $\hat d$ and
$(\hat x,\hat y)$ are supervised against, not the readout shape.
The learnable parameters of the model are
\begin{equation*}
\big\{\hat W^{\mathrm{rec}}_{ij},\;
\{\tilde v_c^{L},\tilde v_c^{R}\}_c,\;
\hat W^{\mathrm{in}}_{:,\theta_0},\,\hat b,\,
\hat W^{\mathrm{out}},\,\hat b^{\mathrm{out}}\big\};
\end{equation*}
the connectome template $W^{\mathrm{con}}$, the afferent
subpopulation membership, the neuron-type assignments, $\tau$ and
$\Delta t$ are given.

\bigskip
\noindent\textbf{Graph neural network model.}\label{method:GNN}
The GNN model replaces the dense recurrent operator
$\sum_j \hat W^{\mathrm{rec}}_{ij}\,\sigma(\hat h_j)$ of
\cref{eq:zhd_dynamics} with a message-passing graph neural network
(Gilmer \emph{et al.}\ 2017), and --- crucially --- does \emph{not}
hard-code either the leak, the firing-rate nonlinearity, or the
synaptic activation function: the linear leak $-\hat h_i/\tau$, the
sigmoid $\sigma$, and the per-edge message that combines a
presynaptic activity with its postsynaptic effect must all be
discovered from the input--output map inside the learned drift
$f_\theta$ and the learned signalling function $g_\phi$. Beyond
removing this functional bias, the GNN also admits an explicit form
of neuronal heterogeneity through a per-neuron learnable embedding
$\mathbf a_i \in \R^d$ that enters both $f_\theta$ and $g_\phi$:
same-type neurons share their architectural role but can specialise
via their embedding, so a single pair of MLPs covers the $34$ fish2
cell types of \cref{sec:anatomy} and all $N = 917$ instance-level
distinctions without any further per-type parameter tying; we use
$d = 4$ as the default. Nodes correspond to neurons and edges to
the non-zero entries of the zebrafish HD connectome template
$W^{\mathrm{con}}$, and the subthreshold activity evolves as
\begin{equation}
\tau\,\frac{d\hat h_i(t)}{dt}
  = f_\theta\!\Big(\hat h_i(t),\,\mathbf a_i,\,
       \sum_{j \in \mathcal N_i}
       \hat W_{ij}\,g_\phi\!\big(\hat h_j(t),\mathbf a_j\big)\Big)
  + \sum_l \hat W^{\mathrm{in}}_{il}\,u_l(t),
\label{eq:zhd_gnn_dynamics}
\end{equation}
with the same readout (\cref{eq:zhd_readout}), swim-gated encoder
(\cref{eq:zhd_swim_gate}), and dynamics constants ($\tau,\Delta t$)
as the RNN. The neighbour set is fixed by the connectome topology,
$\mathcal N_i = \{j : W^{\mathrm{con}}_{ij} \neq 0\}$. The node
update $f_\theta$ and edge message $g_\phi$ are three-layer
perceptrons (width 64, ReLU, linear output). $g_\phi$ maps the
presynaptic state $\hat h_j$ to a per-edge message conditioned on
$\mathbf a_j$ and weighted by the per-edge gain $\hat W_{ij}$;
$f_\theta$ then processes the postsynaptic state $\hat h_i$, the
embedding $\mathbf a_i$, and the aggregated synaptic drive. We
constrain $g_\phi$ to be monotonically increasing in $\hat h_j$ via
the prior $\partial g_\phi / \partial \hat h \ge 0$ (see
\cref{eq:zhd_gnn_loss}); combined with the sign-locked $\hat W_{ij}$
(\cref{eq:zhd_gnn_w}) this restores Dale's law at the per-edge level
without introducing a $\pm$-sign degeneracy. The leak, the
firing-rate non-linearity, and any departure from a linear synapse
must emerge from $(f_\theta, g_\phi, \mathbf a)$ themselves; the
monotonicity priors below provide the minimum inductive bias for
$f_\theta, g_\phi$ to behave like a neuron (postsynaptic drive
increases with net excitatory input). The per-edge weight is
sign-locked to the connectome as in the RNN,
\begin{equation}
\hat W_{ij}
  = |W_{ij}|\,\mathrm{sign}(W^{\mathrm{con}}_{ij}),
\label{eq:zhd_gnn_w}
\end{equation}
where $W_{ij} \in \R$ is the trainable per-edge magnitude.
In the zebrafish circuit $\mathrm{sign}(W^{\mathrm{con}}_{ij})$ is
negative for every edge whose presynaptic neuron is in the dIPN
(\emph{IPNd*} or \emph{IPNds*}) and positive for every edge from
\emph{RIPN*} or \emph{pt-IPN*}; the GNN therefore inherits the
inhibitory dIPN directly from the sign pattern. The learnable parameters are
\begin{equation*}
\big\{\theta,\,\phi,\,\mathbf a,\,\hat{W}_{ij},\;
\{\tilde v_c^{L},\tilde v_c^{R}\}_c,\;
\hat W^{\mathrm{in}}_{:,\theta_0},\,\hat b,\,
\hat W^{\mathrm{out}},\,\hat b^{\mathrm{out}}\big\};
\end{equation*}
the connectome template $W^{\mathrm{con}}$, the afferent
subpopulation membership, the neuron-type assignments, $\tau$ and
$\Delta t$ are given.

As with the RNN, heading recovery from cos/sin readouts is
fundamentally non-unique: many distinct $(\theta,\phi,\mathbf a,W)$
configurations produce identical readouts. We constrain the solution
space with the same connectome-prior regularisers as the RNN
($\mathcal{L}_{\mathrm{cos}}$, $\mathcal{L}_{\mathrm{nf}}$ from
\cref{eq:zhd_cos,eq:zhd_nf}, computed on the per-edge gain surface
$\hat W_{ij}$ with floor threshold $\kappa$), together with sparsity
and monotonicity priors on the MLPs:
\begin{align}
\mathcal{L} \;=\;&
  \tfrac{1}{BT}\sum_{b,t}\|\hat y_{b,t} - y_{b,t}\|_2^2
+ \lambda_{\mathrm{cos}}\mathcal{L}_{\mathrm{cos}}
+ \lambda_{\mathrm{nf}}\mathcal{L}_{\mathrm{nf}} \nonumber\\
& +\;\lambda_0\|\theta\|_1 + \lambda_1\|\phi\|_1
   + \lambda_2\|\theta\| + \lambda_3\|\phi\|
   \;+\;\gamma_0\|W\|_1 + \gamma_1\|W\|_2 \nonumber\\
& +\;\mu_0\!\left\|
       \relu\!\left(+\tfrac{\partial f_\theta(\hat h,\mathbf a,m)}
                          {\partial \hat h}\right)\right\|
   +\;\mu_1\!\left\|
       \relu\!\left(-\tfrac{\partial f_\theta(\hat h,\mathbf a,m)}
                          {\partial m}\right)\right\| \nonumber\\
& +\;\mu_2\!\left\|
       \relu\!\left(-\tfrac{\partial g_\phi(\hat h,\mathbf a)}
                          {\partial \hat h}\right)\right\|
   +\;\mu_3\,\big\|g_\phi(h_\star,\mathbf a)-h_\star\big\|,
\label{eq:zhd_gnn_loss}
\end{align}
where $m = \sum_{j \in \mathcal N_i}\hat W_{ij}\,
g_\phi(\hat h_j,\mathbf a_j)$ is the aggregated synaptic input. The
$\ell_1$ penalties $\lambda_0,\lambda_1$ on the MLP weights bias
$f_\theta, g_\phi$ toward sparser, simpler functions; the $\ell_2$
penalties $\lambda_2,\lambda_3$ prevent weight explosion. The
leak-monotonicity term $\mu_0$ penalises \emph{positive} slopes of
$f_\theta$ in the postsynaptic state $\hat h$, encoding the prior
that the drift contains a leak
($\partial f_\theta / \partial \hat h \le 0$); $\mu_1$ penalises
negative slopes of $f_\theta$ in the aggregated input $m$, encoding
the prior that postsynaptic drive increases with net excitatory
input. Together, $\mu_0$ and $\mu_1$ force the discovered drift to be
decreasing in self-voltage and increasing in synaptic input --- the
two qualitative signatures of \cref{eq:zhd_dynamics}. The term $\mu_2$
enforces the analogous monotonicity prior on $g_\phi$ in the
presynaptic voltage. The normalisation term $\mu_3$ anchors $g_\phi$
at a reference subthreshold value $h_\star$, removing the
multiplicative degeneracy between $g_\phi$ and $\hat W$ in
\cref{eq:zhd_gnn_dynamics}. Hyperparameters
$\lambda_{\{0,1,2,3\}},\,\gamma_{\{0,1\}},\,\mu_{\{0,1,2,3\}}$ and the
MLP widths/depths are tuned with an agentic optimisation procedure,
with $r_{\mathrm{roll},1k}$ --- the Pearson correlation between the decoded
and true heading over a $1\,000$-step deterministic swim-impulse rollout
--- as the primary score.

\bigskip
\noindent\textbf{Swim-integration task.}%
\label{method:swim_task_modes}\label{method:xi_leaky}
The on-disk swim-integration dataset is a \emph{single superset} from
which several sub-tasks are projected at training time. Swim events are
sampled at a Poisson rate $r_{\mathrm{swim}}=0.5\,\mathrm{Hz}$ with
event duration $\Delta t_{\mathrm{swim}}=0.3\,$s, partitioned into four
behavioural categories with fractions $L:R:F:B=0.3:0.3:0.2:0.2$. Left
and right events drive an angular signed phase impulse, forward and
backward events drive a signed forward-velocity impulse. The input is a
$4$-channel stream $u\in\R^{B\times T\times 4}=[\,\omega,\,v_{\mathrm{fwd}},\,
\cos\theta_0\!\cdot\!\delta_{t,0},\,\sin\theta_0\!\cdot\!\delta_{t,0}\,]$:
left/right events drive $\omega$, forward/backward events drive
$v_{\mathrm{fwd}}$, and the initial heading $(\cos\theta_0,\sin\theta_0)$
enters as a single-frame impulse at $t=0$ (position needs no such cue, as it
starts at the origin). The target column layout depends on the sub-task: the
\emph{scalar} recipe stores $y=[\,\cos\theta,\,\sin\theta,\,d\,]$ with
$\theta(t)=\theta_0+\int_0^t\omega\,ds$ and $d(t)=\int_0^t v_{\mathrm{fwd}}\,ds$
($d(0)=0$), while the \emph{2D path-integration} recipe stores
$y=[\,\cos\theta,\,\sin\theta,\,x,\,y\,]$, projecting $v_{\mathrm{fwd}}$ through
the current heading and integrating each spatial axis independently
(\cref{eq:xy_pi}). The 2D recipe is the \emph{true} path-integration target: it
couples translation to heading, so the network must maintain $\theta$
internally to predict $(x,y)$.

The default scalar $d$ and the default 2D position $(x, y)$ are both
\emph{cumulative} integrators of their respective drives. With a non-zero
mean forward-swim drive these targets grow linearly with the
curriculum horizon, so the loss scales with $T$ and cross-snapshot
comparisons become awkward. A finite time constant $\tau>0$ replaces
the cumulative sum with the leaky recurrence
\begin{equation}
d(t+\Delta t) = \left(1-\frac{\Delta t}{\tau_d}\right)d(t)
    + v_{\mathrm{fwd}}(t)\,\Delta t,
\qquad d(0)=0,
\label{eq:xi_leaky}
\end{equation}
equivalent to $\dot d = -d/\tau_d + v_{\mathrm{fwd}}$. For the
2D-position target the same leaky law is applied independently to each
axis,
\begin{equation}
\frac{dx}{dt} = -\frac{x}{\tau_p} + v_{\mathrm{fwd}}\cos\theta,
\qquad
\frac{dy}{dt} = -\frac{y}{\tau_p} + v_{\mathrm{fwd}}\sin\theta,
\qquad x(0)=y(0)=0,
\label{eq:xy_pi}
\end{equation}
which keeps the trajectory bounded near the recent egocentric
displacement. Biologically this is the natural choice: real neural
integrators leak, and what the fish actually encodes is a
low-pass-filtered estimate of \emph{recent} forward swim vigour and
recent displacement, not absolute long-horizon position. The
production runs use $\tau_d=\tau_p=0.5\,$s, with the
cumulative-integrator recipes preserved as separate variants so the loss
and metric comparisons stay clean.

\bigskip
\noindent\textbf{Proprioceptive-gain mismatch task.}\label{method:mismatch}
The proprioception circuit (\cref{method:proprioception}) lets us pose a
sharper question than redundant sensory$+$efference integration: can the
circuit track a \emph{discrepancy} between its two angular streams? We
drive ARTR with the observed angular velocity $\omega$ and
motor\_efferent with a \emph{gain-modulated} proprioceptive copy
$\omega_{\mathrm{proprio}}(t) = g(t)\,\omega(t)$, where the gain $g(t)$
is a piecewise-constant process drawn uniformly in $[0,1.5]$ that steps
to a new value every ${\approx}2\,$s --- a discrete proprioceptive-gain
regime switch, in the spirit of the closed-loop gain manipulations of
the ZAPBench \textsc{gain} block. The supervised third readout column is
no longer a forward distance but the integrated mismatch
\begin{equation}
m(t) = \int_0^t \big(\omega(s) - \omega_{\mathrm{proprio}}(s)\big)\,ds
     = \int_0^t \big(1 - g(s)\big)\,\omega(s)\,ds,
\label{eq:zhd_mismatch}
\end{equation}
the accumulated sensory-vs-proprioceptive heading discrepancy --- zero
when proprioception is veridical ($g\equiv1$) and growing whenever the
two streams disagree. The input is the $5$-channel proprioception layout
$[\omega, v_{\mathrm{fwd}}, \omega_{\mathrm{proprio}},
\cos\theta_0\delta_{t_0}, \sin\theta_0\delta_{t_0}]$ ($n_{\mathrm{in}}=5$)
and the readout is $\hat y=(\cos\hat\theta,\sin\hat\theta,\hat m)$
($n_{\mathrm{out}}=3$ --- the same column count as the
heading$+$distance recipe of \cref{eq:zhd_readout}, but with the third
column supervised against $m$). Recovering $m$ forces the recurrent
circuit to maintain a running difference of its two anatomically
distinct afferent streams rather than echo either one.

\begin{figure}[t!]
\centering
\includegraphics[width=\textwidth]{\figdir/fig_3_swim_task.png}
\caption{\textbf{Swim-integration task and its target recipes for the
same trial.} \textbf{(a)}~$4$-channel input:
$\omega$, $v_{\mathrm{ext}}$, and the initial-heading impulse
$(\cos\theta_0,\sin\theta_0)\,\delta_{t,0}$ (non-zero only at $t=0$;
shown as stem markers carrying the per-trial value).
\textbf{(b)}~Scalar-$d$ target: $\cos\theta$, $\sin\theta$, and
forward distance $d$; perfect cumulative integrator (green) vs leaky
$\tau_d=0.5\,$s (red dashed).
\textbf{(c)}~The same trial's 2D trajectory in spatial coordinates,
computed from the $\omega$ and $v_{\mathrm{ext}}$ inputs of (a):
cumulative path (green) and leaky path (red dashed, $\tau_p=0.5\,$s).
\textbf{(d)}~Proprioceptive-gain mismatch task on the same $\omega$
(\cref{method:mismatch}): a piecewise-constant gain $g(t)\in[0,1.5]$
(top) sets the proprioceptive copy $\omega_{\mathrm{proprio}}=g\,\omega$
(orange, middle) against the sensory $\omega$ (green); the supervised
scalar (bottom) is the integrated mismatch
$\int(\omega-\omega_{\mathrm{proprio}})\,dt$.}
\label{fig:zhd_swim_task_modes}
\end{figure}

\paragraph{Training.}

\bigskip
Training optimises a per-frame MSE
on the heading readout with two connectome-prior regularisers and
an $\ell_1$ sparsity prior on the recurrent magnitudes,
\begin{equation}
\mathcal{L}
= \tfrac{1}{BT}\sum_{b,t} \|\hat y_{b,t} - y_{b,t}\|_2^2
+ \lambda_{\mathrm{cos}}\,\mathcal{L}_{\mathrm{cos}}
+ \lambda_{\mathrm{nf}}\,\mathcal{L}_{\mathrm{nf}}
+ \lambda_{1}\,\|\hat{W}^{rec}_{ij} \|_1,
\label{eq:zhd_loss}
\end{equation}
with block losses computed over type-pair masks
$\mathcal{B}_p \subset \{1,\dots,N\}^2$ (one block per ordered
pre/post neuron-type pair $p \in \mathcal{P}$),
\begin{align}
\mathcal{L}_{\mathrm{cos}}
  &= \tfrac{1}{|\mathcal{P}|}\sum_{p \in \mathcal{P}}
     \!\left[1 - \frac{\langle \hat W^{\mathrm{rec}}_{\mathcal{B}_p},\,
         W^{\mathrm{con}}_{\mathcal{B}_p}\rangle}
       {\|\hat W^{\mathrm{rec}}_{\mathcal{B}_p}\|\,
        \|W^{\mathrm{con}}_{\mathcal{B}_p}\|}\right],
\label{eq:zhd_cos}\\
\mathcal{L}_{\mathrm{nf}}
  &= \tfrac{1}{|\mathcal{P}|}\sum_{p \in \mathcal{P}}
     \relu\!\big(\kappa -
       \overline{|\hat W^{\mathrm{rec}}_{\mathcal{B}_p}|}\big)^2.
\label{eq:zhd_nf}
\end{align}
The cosine-distance term $\mathcal{L}_{\mathrm{cos}}$ anchors each
block to the connectome template; the norm-floor term
$\mathcal{L}_{\mathrm{nf}}$ prevents any type-pair block from
collapsing to zero by penalising the squared shortfall of the block
mean absolute weight $\overline{|\hat W^{\mathrm{rec}}_{\mathcal{B}_p}|}$
below a fixed floor $\kappa$ (typical value $\kappa = 0.05$). Because
$\mathcal{L}_{\mathrm{cos}}$ is scale-invariant,
$\mathcal{L}_{\mathrm{nf}}$ is required to prevent a degenerate
solution in which the cosine prior is satisfied by an arbitrarily
small recurrent magnitude. The type-pair vocabulary $\mathcal{P}$ is
the cartesian product of the 34 fish2 cell types identified in
\cref{sec:anatomy}, restricted to pairs with at least one non-zero
entry in $W^{\mathrm{con}}$.

\bigskip
\noindent\textbf{Calcium observation loss.}\label{method:obs}
The heading loss of \cref{eq:zhd_loss} constrains only the
two-dimensional readout $\hat y(t)$; the $N$-dimensional recurrent state
$\hat h(t)$ is left unobserved, so the task alone does not pin the
internal dynamics. To constrain the implementation we add an optional
term that matches the model's predicted calcium to \emph{recorded}
activity. A GCaMP indicator turns the model's per-neuron activity into a
fluorescence trace by causal convolution with a unit-area
difference-of-exponentials kernel,
\begin{equation}
k_{\mathrm{GCaMP}}(t) \;\propto\; e^{-t/\tau_d} - e^{-t/\tau_r},
  \quad t \ge 0,
\qquad
\hat c_i(t) \;=\; \big(k_{\mathrm{GCaMP}} * \hat h_i\big)(t),
\label{eq:zhd_gcamp}
\end{equation}
with nominal cytosolic GCaMP7f rise/decay constants
$(\tau_r,\tau_d) = (0.15,\,1.20)\,\mathrm{s}$ \citep{dana2019}. ZAPBench
imaged the slower nuclear \emph{H2B-GCaMP7f}, so \cref{eq:zhd_gcamp} if
anything \emph{under}-states the low-pass smearing of the real observable.
The kernel is normalised to unit area so the transform
preserves the mean, the convolution is evaluated on the model grid
$\Delta t$, and it is differentiable, so gradients flow from the
fluorescence back into $\hat W^{\mathrm{rec}}$, the afferent gate and
$\hat b$. Let $\mathcal{O}$ index the $|\mathcal{O}| = 481$ HD-circuit
neurons matched to a recorded ROI (below) and let $c^{\star}_i(t)$ be the
recorded $\Delta F/F$ of neuron $i$. Because the indicator gain and
baseline are not identifiable, the term is made invariant to a per-neuron
affine rescaling by $z$-scoring both signals over time before comparison,
\begin{equation}
\mathcal{L}_{\mathrm{obs}}
  = \frac{1}{|\mathcal{O}|\,B_c T}\sum_{i\in\mathcal{O}}\sum_{b,t}
    \big(z[\hat c_i]_{b,t} - z[c^{\star}_i]_{b,t}\big)^2 ,
\qquad
z[x]_{b,t} = \frac{x_{b,t} - \mu_{b,i}}{\sigma_{b,i}},
\label{eq:zhd_obs}
\end{equation}
where $\mu_{b,i},\sigma_{b,i}$ are the per-trial, per-neuron mean and
standard deviation over time. For unit-variance signals
$\mathcal{L}_{\mathrm{obs}} = 2\,(1-\rho)$ with $\rho$ the Pearson
correlation, so the term rewards temporal \emph{shape} agreement rather
than absolute scale. The full objective adds it to \cref{eq:zhd_loss}
with weight $\lambda_{\mathrm{obs}}$,
$\mathcal{L}_{\mathrm{total}} = \mathcal{L}
  + \lambda_{\mathrm{obs}}\,\mathcal{L}_{\mathrm{obs}}$;
setting $\lambda_{\mathrm{obs}} = 0$ (or supplying no calcium trials)
recovers the task-only model exactly.

\paragraph{Mutual-information estimators.}\label{method:MI}
We use mutual information to quantify how strongly each unit's
firing rate $r_i(t) = \sigma(\hat h_i(t))$ tracks the heading
$\theta(t)$ (or, where stated, the angular velocity $\omega(t)$ or
the swim-onset trace $s(t)$) over a held-out rollout. For a single
neuron we wrap $\theta$ to $(-\pi, \pi]$ via
$\theta \leftarrow \arg(e^{i\theta})$, discretise the wrapped angle
into $n_\theta = 32$ equal-width angular bins and $r$ into $n_r = 20$
equal-width bins, then compute the plug-in estimator in bits,
\begin{equation}
\hat I(r_i;\theta) = \sum_{b,c} \hat p(b,c)\,\log_2
\frac{\hat p(b,c)}{\hat p(b)\,\hat p(c)},
\label{eq:zhd_mi_plugin}
\end{equation}
where $\hat p$ is the empirical joint over the
$(\theta\!\!-\!\!\mathrm{bin}, r\!\!-\!\!\mathrm{bin})$ histogram. The
plug-in construction is used so that the per-neuron and
per-cell-type quantities of
\cref{sec:results_four_classes} are
comparable across cell types and sub-populations. For a group of $K$ neurons we
use the cross-validated lower bound
\begin{equation}
\hat I_{\mathrm{LB}}(\mathbf r_{1:K};\theta) =
H(\theta) - \tfrac{1}{|\mathcal D_{\mathrm{te}}|}
\sum_{(t,\theta)\in\mathcal D_{\mathrm{te}}}
-\log_2 q_\psi(\theta_b\mid \mathbf r(t)),
\label{eq:zhd_mi_joint_lb}
\end{equation}
where $q_\psi$ is an $L_2$-regularised multinomial logistic
regression with input $\mathbf r \in \mathbb R^K$ and output the
$n_\theta$ heading bins, trained on a stratified 5-fold split.

\iffalse  % --- Image-domain ROI sampling / dF/F0 paragraph commented out ---
\paragraph{Image-domain ROI sampling and $\Delta F/F_0$.}\label{method:dff}
To make the model's population dynamics comparable to two-photon
calcium imaging of the larval-zebrafish dIPN, we sample activity
\emph{from the rendered frames} of the dorsal-view animation, not
from the latent state $\hat h_i(t)$. Each frame is a
$3000 \times 4380$-pixel canvas in which each neuron's skeleton is
drawn with a per-segment alpha proportional to its firing rate; we
place four axis-aligned ROI rectangles $R_k$
($k \in \{\mathrm{dIPN}_L, \mathrm{dIPN}_R, \mathrm{dsIPN}_L,
\mathrm{dsIPN}_R\}$) over the dorsal-IPN neuropil (drawn on frame~0
of \emph{\figdir/3D\_voltage\_const/frame\_0000.png}) and
integrate the green channel of the canvas RGBA buffer:
\begin{equation}
I_k(t) \;=\; \frac{1}{|R_k|}\sum_{(x,y)\,\in\,R_k}
              \mathrm{img}_t[y, x, \mathrm{green}].
\label{eq:zhd_roi_intens}
\end{equation}
The four traces $I_k(t)$ are then converted to a fractional change
with respect to a post-init baseline, mirroring the Seelig / Kim /
Green calcium-imaging convention:
\begin{align}
F_0[k] &\;=\; \bigl\langle I_k(t)\bigr\rangle_{t \,\ge\,
                t_{\mathrm{init}}} + \varepsilon, \\
\Delta F/F_0\,(k,t) &\;=\;
   \frac{I_k(t) - F_0[k]}{F_0[k]},
\label{eq:zhd_dff}
\end{align}
with $t_{\mathrm{init}} = 0.5$~s (drops the network start-up
transient that would otherwise dominate the baseline) and
$\varepsilon = 10^{-6}$. The per-pair population-vector position
overlaid on the kinographs in red is the activity-weighted centre
of mass across each $L/R$ pair,
$\mathrm{pos}_p(t) = \big(I_{p,L}\,y_{p,L} + I_{p,R}\,y_{p,R}\big)/
                      \big(I_{p,L} + I_{p,R}\big)$,
i.e.\ the same Seelig/Hulse PVA reduced to two phase bins per pair.
Image-domain sampling inherits all the projection biases of the
microscope (depth-of-field, projection, downsample) and so the
kinographs are directly comparable to \petrucco{}~Fig.~2 without
having to invert the calcium kernel.
\fi  % --- end commented-out ROI / dF/F0 paragraph ---

\section{Results}
\label{sec:results}

\subsection{Trained heading integrator}
\label{sec:results_evolution}

We train the sign-locked RNN of \cref{method:RNN} on the nominal
$917$-cell circuit of \cref{fig:zhd_3d}\textbf{c} --- angular drive
$\omega$ routed exclusively to ARTR (\emph{RIPN01/02/03\_a/03\_b})
--- on the
heading-only sub-task of \cref{fig:zhd_swim_task_modes} ($n_{\rm in}=3$
channels, $n_{\rm out}=2$ outputs).
\Cref{fig:zhd_evolution_artr_pt1} reports the trained network.
After training the heading code is correctly recovered. Across the
five held-out swim-integration test trials (panel \textbf{i}) the
decoded heading tracks ground truth with a mean per-trial circular
RMSE of $5.9^\circ \pm 2.5^\circ$ and Pearson $r=0.997 \pm 0.003$.
Constant-$\omega$ deterministic rollouts of $T=2000$ frames spanning
the full $\pm 180^\circ/\mathrm{s}$ range (panels \textbf{h},
\textbf{j}) integrate heading at a near-unit gain: the linear-fit slope
of decoded vs.\ true $\omega$ is $1.07$ (mean local gain
$1.04 \pm 0.05$; panel \textbf{h}), and the Pearson correlation on the
unwrapped trajectories is $\ge 0.999$ for every
sweep. The dIPN carries a single travelling
ring activity peak (panel \textbf{e}). The learned recurrent weights
$\hat W^{\mathrm{rec}}$ sit on the signed connectome support by
construction; the per-edge magnitude correlates with the connectome
template at Pearson $r=0.430$ (panel \textbf{c}), consistent with the
sign-locked re-scaling described in \cref{method:RNN}.

\begin{figure}[p]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_evolution_zebrafish_hd_si_ipn_917_v1_selfmotion_rotation.png}
  \caption{\textbf{Trained heading integrator on the ARTR / pt-IPN1
  variant of \cref{fig:zhd_3d}\textbf{c}.} Sign-locked RNN with
  $\hat W^{\mathrm{rec}} = |\hat S|\odot\mathrm{sign}(W^{\mathrm{con}})$
  on the nominal $917$-cell circuit ($917$ v1, inhibitory). Drive:
  $\omega$ routed exclusively to ARTR
  (\emph{RIPN01/02/03\_a/03\_b}). Task: heading only ($n_{\rm in}=3$,
  $n_{\rm out}=2$).
  \textbf{(a)} GT connectome $W^{\mathrm{con}}$, neuron-type blocks on
  both axes.
  \textbf{(b)} Learned $\hat W^{\mathrm{rec}}$ on the same scale.
  \textbf{(c)} Scatter of the learned per-edge weight $\hat W_{ij}$
  against $W^{\mathrm{con}}_{ij}$ over all connectome edges.
  \textbf{(d)} All $917$ neurons over a constant-$\omega =
  +60^\circ/\mathrm{s}$ rollout, sorted by cell type.
  \textbf{(e)} dIPN sub-population over the same rollout; firing rates
  $z$-scored per neuron.
  \textbf{(f)} Constant-$\omega$ rollout. Top: $\omega(t)$. Bottom:
  true heading (green) and decoded heading (black), wrapped. Title
  reports post-warmup Pearson $r$ and circular RMSE.
  \textbf{(g)} Same layout on a held-out swim-integration test trial.
  \textbf{(h)} Integration gain (Hulse-style scatter): measured slope
  of the unwrapped decoded heading vs.\ true $\omega$, on a
  $15^\circ/\mathrm{s}$ grid over $[-180,+180]$; the
  near-linear domain is shaded, dashed line $g = 1$.
  \textbf{(i)} Five held-out swim-integration test trials: true
  heading (green) and decoded heading (black); per-panel circular RMSE
  and Pearson $r$.
  \textbf{(j)} Five constant-$\omega$ deterministic sweeps
  ($T=2000$ frames): same overlay; per-panel circular RMSE and Pearson
  $r$ on the unwrapped trajectories.}
  \label{fig:zhd_evolution_artr_pt1}
\end{figure}

\paragraph{Decoder choice shapes the recurrent representation.}
\label{sec:results_decoder}

In \cref{sec:results_evolution} the heading was read out by a linear map
onto $(\cos\theta,\sin\theta)$ and decoded with $\operatorname{atan2}$.
The readout is not a neutral observer: because the recurrent weights are
connectome-locked and only re-scaled, the \emph{form} of the decoder is
one of the few degrees of freedom left to the optimiser, and it can
shape the internal representation the network settles on. We therefore
re-train the same $917$-cell circuit on the same heading task with two
alternative heading decoders, holding the recurrent dynamics and the
afferent gate fixed: (i) a \emph{$64$-bin softmax} readout, in which the
heading is one of $64$ angular bins (a localist ring code) decoded by the
softmax-weighted circular mean ($n_{\mathrm{out}}=64$); and (ii) a
\emph{nonlinear MLP} readout replacing the linear map.

The $64$-bin decoder converges as well as the $(\cos\theta,\sin\theta)$
readout: heading is recovered across held-out trials and constant-$\omega$
sweeps, and the dIPN still carries a single travelling activity peak, now
read out directly as a moving band in bin space
(\cref{fig:zhd_evolution_kbins}, panel \textbf{h}). The nonlinear MLP
decoder, by contrast, \emph{fails to converge} on this circuit --- the
network does not settle on a stable heading code --- so we report it only
as a negative result and defer its analysis to future work. The takeaway
is methodological: with the recurrent substrate fixed to the connectome,
the decoder is an active modelling choice that interacts with the
dynamics. A too-flexible readout (the MLP) can prevent the circuit from
finding the integrator at all, whereas a localist bin readout recovers it
as cleanly as the cos/sin one --- and, as the ARTR diagnostic confirms
(\cref{fig:zhd_artr_oscillation_kbins}), leaves the recurrent dynamics
qualitatively unchanged.

\begin{figure}[p]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_evolution_zebrafish_hd_si_ipn_917_v1_selfmotion_rotation_kbins.png}
  \caption{\textbf{Heading integrator with a $64$-bin softmax decoder.}
  Same circuit and heading task as \cref{fig:zhd_evolution_artr_pt1},
  with the linear $(\cos\theta,\sin\theta)$ readout replaced by a
  $64$-bin heading-bin readout ($n_{\mathrm{out}}=64$). Panels
  \textbf{(a)}--\textbf{(e)} follow \cref{fig:zhd_evolution_artr_pt1}
  (GT and learned $\hat W^{\mathrm{rec}}$, per-edge weight scatter,
  all-neuron and dIPN kinographs). \textbf{(f)}~Constant-$\omega$
  rollout: $\omega(t)$ and the bin-decoded heading vs.\ truth.
  \textbf{(g)}~Held-out swim trial, same overlay. \textbf{(h)}~The
  $64$-bin softmax readout over the rollout (bin index vs.\ time; the
  diagonal band is the travelling heading estimate, GT bin overlaid).
  \textbf{(i)}~Five held-out trials and \textbf{(j)}~five constant-$\omega$
  sweeps, bin-decoded heading vs.\ truth.}
  \label{fig:zhd_evolution_kbins}
\end{figure}

\paragraph{Proprioceptive-gain mismatch on the dual-afferent circuit.}
\label{sec:results_mismatch}

The proprioception circuit (\cref{fig:zhd_3d}\textbf{d}) routes the
angular drive through two anatomically distinct afferent streams --- the
sensory ARTR copy $\omega$ and a gain-modulated motor-efference copy
$\omega_{\mathrm{proprio}}=g(t)\,\omega$
(\cref{method:mismatch}; \cref{fig:zhd_swim_task_modes}\textbf{d}).
Trained to report the integrated mismatch
$m=\int(\omega-\omega_{\mathrm{proprio}})\,dt$ alongside heading, the same
sign-locked $917$-cell RNN recovers both. Unlike the heading, which is
encoded as the unit vector $(\cos\theta,\sin\theta)$ and decoded by
$\operatorname{atan2}$, the mismatch $m$ is an unbounded cumulative
quantity and is supervised directly as a scalar --- a plain regression
head trained under the same per-frame MSE. Across held-out swim trials the
decoded heading and the decoded mismatch each track ground truth at
per-trial Pearson $r\approx0.999$, and the mismatch trace follows the
piecewise-constant gain regime-switches frame by frame
(\cref{fig:zhd_evolution_mismatch}, panel \textbf{g} and row \textbf{j}).
The circuit therefore does not merely echo one afferent stream: it
maintains a running \emph{difference} of the two, accumulating the
sensory-vs-proprioceptive heading discrepancy --- a quantity that is zero
only when proprioception is veridical. This is the connectome-locked
analogue of a forward-model error signal, and it shows the dual-afferent
wiring can support gain-mismatch detection, not just redundant
integration.

\begin{figure}[p]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_evolution_zebrafish_hd_si_ipn_917_v1_propriocep_mismatch.png}
  \caption{\textbf{Trained proprioceptive-gain mismatch integrator.}
  Sign-locked $917$-cell RNN on the proprioception circuit
  (\cref{fig:zhd_3d}\textbf{d}), driven by the $5$-channel proprioception
  input and supervised on $(\cos\theta,\sin\theta,m)$ with $m$ the
  integrated mismatch (\cref{method:mismatch}). Panels
  \textbf{(a)}--\textbf{(e)} and \textbf{(h)} follow
  \cref{fig:zhd_evolution_artr_pt1} (GT and learned
  $\hat W^{\mathrm{rec}}$, per-edge weight scatter, all-neuron and dIPN
  kinographs, integration-gain scatter).
  \textbf{(f)}~Constant-$\omega$ deterministic rollout: $\omega(t)$ and
  decoded vs.\ true heading.
  \textbf{(g)}~Mismatch rollout --- top: the sensory $\omega$ and the
  proprioceptive $\omega_{\mathrm{proprio}}=g\,\omega$; middle: the two
  diverging integral paths $\int\omega$ and $\int\omega_{\mathrm{proprio}}$;
  bottom: decoded (black) vs.\ true (green) integrated mismatch $m$.
  \textbf{(i)}~Five held-out swim trials, decoded vs.\ true heading.
  \textbf{(j)}~The same trials, decoded vs.\ true integrated mismatch
  $m$ --- the companion of panel \textbf{g} over multiple trials.
  \textbf{(k)}~Constant-$\omega$ deterministic heading sweeps.}
  \label{fig:zhd_evolution_mismatch}
\end{figure}

\subsection{Trained heading + distance integrator}
\label{sec:results_variants}

We do not know a priori which integration the larval circuit actually
performs --- whether it maintains a scalar forward distance or a full
2-D position, and whether either is accumulated losslessly or leaked.
Rather than commit, we train all four combinations ---
(distance vs.\ 2-D path) $\times$ (cumulative vs.\ leaky) --- on
the same connectome-locked circuit and compare. This brackets the
biologically plausible options: the cumulative target is the idealised
path integrator, the leaky target the bounded \emph{recent-displacement}
estimate a real leaky neural integrator would hold.

The same $917$-cell circuit and sign-locked RNN trains successfully
on every sub-task projection of the swim-integration superset
(\cref{fig:zhd_swim_task_modes}). Each variant changes only the
target column layout ($\hat W^{\mathrm{out}}$, \cref{method:output_readout})
and, where applicable, the leaky-integrator time constant
$\tau_d=\tau_p=0.5\,$s; the gate, connectome and recurrent dynamics
are unchanged across all five runs. The quantitative
trajectory-precision metrics below are aggregated from held-out test
trials on each trained checkpoint, with constant-$\omega$ and
constant-$v_{\mathrm{fwd}}$ deterministic rollouts at $T=2000$ frames
as the extrapolation probe.
\Cref{tab:zhd_subtask_metrics} summarises the headline numbers; full
panel layout in each figure matches \cref{fig:zhd_evolution_artr_pt1}.

\begin{table}[H]
\centering
\small
\begin{tabular}{l c c c}
\toprule
Variant & $r_\theta$ (HD) & $r_d$ (distance) & $\bar r_{xy}$ (2-D) \\
\midrule
Heading                      & $0.998 \pm 0.023$ & ---               & ---               \\
Heading $+$ distance (cumulative)
                             & $0.995 \pm 0.035$ & $0.989 \pm 0.053$ & ---               \\
Heading $+$ distance (leaky) & $0.997 \pm 0.033$ & $0.997 \pm 0.027$ & ---               \\
2-D path (cumulative)        & ---               & ---               & $0.760 \pm 0.268$ \\
2-D path (leaky)             & ---               & ---               & $0.945 \pm 0.152$ \\
\bottomrule
\end{tabular}
\caption{Per-trial Pearson $r$ (mean $\pm$ SD across all $n=10\,000$
held-out test trials, NaN-trial removed). $r_\theta$: correlation of
unwrapped decoded vs.\ ground-truth heading after a $10$-frame warmup.
$r_d$: correlation of decoded vs.\ ground-truth forward distance $d$.
$\bar r_{xy}$: mean of per-axis Pearson on $(x,y)$. The 2-D path is the
hardest target, and the leaky recipe recovers it markedly better than
the cumulative one (\cref{sec:results_variants}).}
\label{tab:zhd_subtask_metrics}
\end{table}

\paragraph{Heading $+$ distance.}
\Cref{fig:zhd_evolution_both,fig:zhd_evolution_both_leaky} train on
the joint heading-plus-forward-distance task
($n_{\rm in}=4$, $n_{\rm out}=3$, target $(\cos\theta,\sin\theta,d)$).
Per-frame test R² is $\ge 0.9998$ on both heading and distance for
both variants. The constant-$\omega$ trajectory metrics over four
sweeps give a mean heading RMSE of $33.1^\circ$ (slope ratio $0.97$)
for the cumulative variant and $8.1^\circ$ (slope ratio $0.99$) for the
leaky variant: \emph{the leaky-target supervision actually produces
a sharper compass} than the cumulative-target version of the joint task
(and is comparable to the heading-only model of
\cref{fig:zhd_evolution_artr_pt1}). The distance head behaves
like the translation-only models: average $d$-RMSE over the five
constant-$v_{\mathrm{fwd}}$ sweeps is $10.9$ (cumulative) and $13.9$
(leaky), with slope-ratio $\dot{\hat d}/\dot d$ of $0.14$ (cumulative)
/ $0.003$ (leaky), confirming that the constant-input extrapolation
problem of the translation-only run is task-, not input-, dependent.

\begin{figure}[p]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_evolution_zebrafish_hd_si_ipn_917_v1_selfmotion_both.png}
  \caption{\textbf{Trained heading $+$ distance integrator,
  cumulative target.} Same panel layout as
  \cref{fig:zhd_evolution_artr_pt1}; panel (g) overlays the GT and
  decoded forward-distance trace below the heading components.}
  \label{fig:zhd_evolution_both}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_evolution_zebrafish_hd_si_ipn_917_v1_selfmotion_both_leaky.png}
  \caption{\textbf{Trained heading $+$ distance integrator,
  leaky target} ($\tau_d=0.5\,$s). Same panel layout as
  \cref{fig:zhd_evolution_both}.}
  \label{fig:zhd_evolution_both_leaky}
\end{figure}

\paragraph{2-D path integration.}
\Cref{fig:zhd_evolution_position2d,fig:zhd_evolution_position2d_leaky}
train on the full 2-D path task ($n_{\rm in}=4$, $n_{\rm out}=4$,
target $(\cos\theta,\sin\theta,x,y)$ with
$\dot x = v_{\mathrm{fwd}}\cos\theta$,
$\dot y = v_{\mathrm{fwd}}\sin\theta$). Heading is still recovered at
test R² $\ge 0.994$ on the disk-stored target. The 2-D-position head
is the harder regression: averaged over five $(\omega, v_{\mathrm{fwd}})$
sweeps at $v_{\mathrm{fwd}}=+1$, the per-frame Euclidean RMSE between
decoded $(\hat x,\hat y)$ and true $(x,y)$ is $2.10$ for the cumulative
variant and $0.98$ for the leaky variant --- the leaky target
\emph{halves} the trajectory error. Mean endpoint error
$\|(\hat x,\hat y)_T - (x,y)_T\|$ similarly drops from $3.72$ to
$1.78$. The path-length ratio
$\sum\|\Delta(\hat x,\hat y)\|/\sum\|\Delta(x,y)\|$ is highly
variable across sweeps (cumulative $0.23$--$1.85$, leaky $0.28$--$0.98$),
indicating that the absolute arc-length is unreliable on deterministic
constant-drive probes even when the per-frame error is small. Panels
(f)--(g) of these figures show the decoded $(x,y)$ trajectory as a
2-D path instead of the heading time-trace.

\begin{figure}[p]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_evolution_zebrafish_hd_si_ipn_917_v1_position_2d.png}
  \caption{\textbf{Trained 2-D path integrator, cumulative target.}
  Panels (a)--(e), (h)--(i) follow \cref{fig:zhd_evolution_artr_pt1};
  panels (f)--(g) show the decoded path $(x,y)$ in spatial coordinates
  on the constant-$\omega$ rollout and on a held-out swim trial.}
  \label{fig:zhd_evolution_position2d}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_evolution_zebrafish_hd_si_ipn_917_v1_position_2d_leaky.png}
  \caption{\textbf{Trained 2-D path integrator, leaky target}
  ($\tau_p=0.5\,$s). Same panel layout as
  \cref{fig:zhd_evolution_position2d}.}
  \label{fig:zhd_evolution_position2d_leaky}
\end{figure}

\paragraph{Leaky vs.\ cumulative: bounded steady-state radius.}
\label{res:leaky_vs_cumulative}
The cumulative and leaky variants of the swim-integration task share the
same recurrent circuit, the same input drive, and the same heading head;
they differ only in the shape of the supervised distance and position
target. The cumulative target is the indefinite integral of the
corresponding velocity, which grows linearly under a sustained constant
drive. The leaky target replaces the integrator with the first-order leak
of \cref{eq:xi_leaky,eq:xy_pi}, whose steady-state response to a constant
drive is bounded.

For the 2-D position target under constant $v_{\mathrm{ext}}$ and constant
angular velocity ($\theta(t) = \omega t$), writing $z = x + iy$ turns
\cref{eq:xy_pi} into the linear ODE
$\dot z = -z/\tau_p + v_{\mathrm{ext}}\,e^{i\omega t}$. Substituting the
steady-state ansatz $z(t) = A\,e^{i(\omega t + \varphi)}$ gives
$A\,e^{i\varphi} = \tau_p\,v_{\mathrm{ext}}/(1 + i\,\tau_p\,\omega)$, so
\begin{equation}
R \;\equiv\; |A| \;=\; \frac{\tau_p\,v_{\mathrm{ext}}}
                            {\sqrt{1 + (\tau_p\,\omega)^{2}}},
\qquad
\varphi \;=\; -\arctan(\tau_p\,\omega).
\label{eq:leaky_radius}
\end{equation}
\Cref{eq:leaky_radius} is the response magnitude and phase of a
first-order low-pass of cutoff $1/\tau_p$ driven by a complex sinusoid at
frequency $\omega$: the DC gain is $\tau_p\,v_{\mathrm{ext}}$, the gain
rolls off by $1/\sqrt{1+(\tau_p\,\omega)^2}$ at frequency $\omega$, and
$\varphi$ is the lag of the decoded position behind the instantaneous
heading.

Two limits of $R$ are useful. For $\tau_p\,\omega \ll 1$ the radius is
$R \approx \tau_p\,v_{\mathrm{ext}}$, the asymptote of the leaky
integrator in the no-rotation limit. For $\tau_p\,\omega \gg 1$ the
radius is $R \approx v_{\mathrm{ext}}/\omega$, the kinematic radius of
the circle traced by a forward-moving rotating object; $\tau_p$ drops
out entirely in this regime.

In \cref{fig:zhd_swim_task_modes}\textbf{(c)} the cumulative path drifts
unboundedly from the origin under the constant $(\omega,
v_{\mathrm{ext}})$ drive, while the leaky path stays inside the disk of
radius $R$ given by \cref{eq:leaky_radius}. For the scalar-distance
variant (\cref{eq:xi_leaky}) the same argument gives
$\tau_d\,v_{\mathrm{ext}}$ as the steady-state forward distance under
constant $v_{\mathrm{ext}}$. The practical consequence for training is
that the supervised target stays $\mathcal{O}(1)$ across the curriculum
horizon, so the per-frame loss carries no $T$-dependent scale and the
distance / position heads are directly comparable across curriculum
stages. The cumulative variants are retained as the unbounded reference
reported alongside the leaky variants in
\cref{tab:zhd_subtask_metrics}.

\paragraph{Summary: per-frame accuracy cannot pick the integrator --- biology must.}
All five runs converge to a working integrator on the same connectome,
and per-frame accuracy is saturated for every one of them
($r_\theta$, $r_d$, $\bar r_{xy}$ all $\ge 0.99$ on held-out trials,
\cref{tab:zhd_subtask_metrics}); the constant-$\omega$ integration gain
also stays unit-slope whether or not forward-distance or 2-D position is
decoded alongside heading. Per-frame task accuracy therefore
\emph{cannot} select among the four target recipes --- the choice is a
question for biology, not for the loss. Two axes are open, and the
evidence points differently on each.

\emph{Leaky vs.\ cumulative is not a binary --- everything biological
leaks.} Integration is a network-level rather than a single-cell
property, and a truly lossless integrator requires a perfectly tuned,
marginally stable attractor with exact unit feedback that is not
physically realisable \citep{seung1996,khona2022}; any real neural
integrator has a finite time constant set by its recurrent gain, and
that leak is empirically tunable --- in the goldfish and zebrafish
oculomotor integrators the persistence time constant can be detuned
across an order of magnitude and is retuned by feedback
\citep{major2004,goncalves2014}. Our own ``cumulative'' runs bear
this out: under a sustained constant drive the trained cumulative
distance head already extrapolates with a gain far below unity (slope
ratio $\approx 0.14$, collapsing further for the leaky target), i.e.\ it
behaves leakily regardless of the training target. The question is thus
not \emph{whether} the circuit leaks but \emph{how} leaky --- the value
of $\tau$. The one exception is heading: the larval \rpi{} bump persists
for tens of seconds without drift \citep{petrucco2023}, the signature of
a ring attractor held marginally stable by its topology rather than by
fine tuning \citep{kim2017,chaudhuri2019}. The soundest configuration is therefore \emph{asymmetric}
--- effectively non-leaky heading alongside finite-$\tau$ translation ---
and the two channels should not share a single leak constant.

\emph{2-D vs.\ distance-only remains open.} The 2-D path target is the
more rigorous task: only it couples translation to the internally
maintained heading --- the defining feature of true path integration ---
whereas distance-only is heading-blind odometry. A vector-summation
path integrator of exactly this kind has been demonstrated in the fly
central complex \citep{lyu2022}, and that the zebrafish connectome can
support the heading$\leftrightarrow$translation coupling is shown by the
recurrent pool's growing \emph{shared} (jointly $\theta$- and $d$-tuned)
population as the task hardens (\cref{sec:results_four_classes}).
Allocentric place coding does exist in the larval zebrafish
\emph{telencephalon} \citep{yang2024}, but no allocentric position
(place- or grid-like) code is established in the anterior-hindbrain /
IPN circuit we model, so the model's 2-D success states what the
\emph{wiring can compute}, not that this circuit holds a 2-D position
estimate. Distance-only is the conservative claim; 2-D the
ambitious-but-unevidenced one.

Both axes make at the task level the point the rest of the paper makes
at the weight level: the input--output map is degenerate. Per-frame
accuracy fixes neither the leak constants nor the dimensionality of the
integrated variable; biology must, and the present evidence says only
\emph{leaky translation alongside near-lossless heading, with the 2-D
question open}.

\subsection{Angular / displacement partition of the recurrent pool}
\label{sec:results_four_classes}

\paragraph{Angular / displacement partition (recurrent pool only).}
The four joint-target variants
(\cref{fig:zhd_evolution_both,fig:zhd_evolution_both_leaky,fig:zhd_evolution_position2d,fig:zhd_evolution_position2d_leaky})
let us ask which \emph{recurrent} neurons carry information about
\emph{which} target. The analysis is restricted to the recurrent
GABAergic substrate (\emph{IPNd*}, \emph{IPNds*}, the \emph{IPN12}
pool, and the \emph{IPN-core} families IPN28/29/31--36; $n=700$ of the
$917$ cells); the afferents (ARTR, pt-IPN1, motor\_efferent) are excluded
by construction, since their activity is driven directly by the
gate-injected $\omega$ or $v_{\mathrm{fwd}}$ and their MI on the
corresponding integrated quantity is trivially high. For each
recurrent neuron $i$ we compute the per-neuron mutual information on
the heading $I(\hat h_i;\theta)$ and on the forward displacement
$I(\hat h_i;d)$ from a concatenation of held-out test trials (circular
MI for $\theta$, plug-in MI for $d$; for the 2-D path models $d$ is the
travelled path length). Splitting each axis at its recurrent-pool
median assigns every cell to one of four quadrants:
\emph{angular} ($I(\hat h_i;\theta) \ge \tau_\theta$, $I(\hat h_i;d) < \tau_d$),
\emph{displacement} ($I(\hat h_i;\theta) < \tau_\theta$, $I(\hat h_i;d) \ge \tau_d$),
\emph{shared} (high on both), and a residual \emph{neither} group
(\cref{fig:zhd_mi_partition}, top row). Across the four models the
jointly-tuned \emph{shared} pool grows steadily with task difficulty
--- from $64$ cells on the heading$+$distance task to $188$ on the
leaky 2-D path task --- i.e.\ the harder the integration, the more of
the recurrent substrate carries \emph{both} codes at once. This
partition is target-symmetric (heading and translation on the same
footing), restricted to the cells that actually \emph{integrate}, and
uses only model activity (no readout weights).

\paragraph{Is the split anatomical?}
Re-colouring the same MI plane by the neuron's anatomical \emph{family}
(\cref{fig:zhd_mi_partition}, second row) and tabulating the
functional-pool composition of each family (third row) and of each
\emph{cell type} (bottom row) tests whether the angular/displacement
division lines up with anatomy. The recurrent substrate spans four
families --- \emph{dIPN} ($n=313$), \emph{dsIPN} ($n=95$), \emph{IPN12}
($n=108$), and \emph{IPN-core} ($n=184$) --- across $24$ fish2 cell
types. No family or cell type is confined to a single quadrant: every
group contributes cells across the MI plane. The composition is
nonetheless type-dependent --- the \emph{IPN12} pool is enriched for
heading-tuned (angular) cells, whereas the \emph{dsIPN}, \emph{dIPN} and
\emph{IPN-core} families carry most of the displacement and shared
cells. The integrated heading and integrated distance are therefore
\emph{not} computed by disjoint anatomical sub-populations; the two
codes are carried by overlapping recurrent neurons drawn from all four
families, with only a graded cell-type bias.

\paragraph{The split is not inherited from the afferents.}
This distributed code is striking given how cleanly the \emph{afferent}
stage separates the two drives. The angular ARTR copy and the
exteroceptive pt-IPN1 translation copy enter through anatomically
distinct ports (\cref{fig:zhd_3d}\textbf{c}), and within ARTR the
\emph{RIPN01}/\emph{RIPN03\_a} pair delivers the angular drive directly
into the \emph{dIPN} and \emph{dsIPN} ring (\cref{sec:anatomy}). If the
recurrent functional split simply echoed this wiring, the
ARTR-recipient ring families (\emph{dIPN}, \emph{dsIPN}) would be the
heading carriers and the translation-recipient pool the displacement
carrier. The MI partition (\cref{fig:zhd_mi_partition}) does not show
this: heading information is spread across all four recurrent families
rather than concentrated in the direct ARTR targets, the most
heading-enriched family (\emph{IPN12}) is not the principal ARTR
recipient, and the \emph{dsIPN} sub-ring --- itself an ARTR (angular)
target --- in fact leans toward the displacement and shared pools. The
clean angular/translation separation present at the afferent stage is
therefore \emph{reorganised} by the recurrent dynamics into a mixed,
distributed code: which recurrent neuron ends up heading- or
displacement-tuned is set by the connectome-locked recurrent
computation, not read off from its afferent-input identity.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_zebrafish_mi_partition.png}
  \caption{\textbf{MI-based partition of the recurrent pool across four
  joint-target models.} Columns, left to right: heading\,$+$\,distance
  (cumulative), heading\,$+$\,distance (leaky), 2-D path (cumulative),
  2-D path (leaky), all on the nominal $917$-cell circuit. Each dot is
  one recurrent neuron --- the GABAergic substrate \emph{dIPN},
  \emph{dsIPN}, \emph{IPN12} and \emph{IPN-core} ($n=700$; afferents
  excluded) --- plotted by its mutual information on heading
  $I(\hat h_i;\theta)$ (circular, $24$ heading bins) versus on forward
  displacement $I(\hat h_i;d)$ (plug-in, $32\times20$ bins; the
  travelled path length for the 2-D models); dashed lines are the
  per-model median thresholds $\tau_\theta,\tau_d$.
  \textbf{(a--d)}~Row 1: dots coloured by functional quadrant ---
  angular (blue, bottom-right), displacement (orange, top-left), shared
  (green, top-right), neither (grey, bottom-left). MI axis titles are
  written once, on panel \textbf{a}.
  \textbf{(e--h)}~Row 2: the \emph{same} neurons coloured by anatomical
  family --- \emph{dIPN} (amber), \emph{dsIPN} (teal), \emph{IPN12}
  (rose), \emph{IPN-core} (purple).
  \textbf{(i--l)}~Row 3: per-family functional-pool composition (stacked
  bars, same pool colours).
  \textbf{(m--p)}~Row 4: the same composition per fish2 cell type ---
  reading off whether the functional split aligns with anatomy.}
  \label{fig:zhd_mi_partition}
\end{figure}

\paragraph{Does the MI add up?}
The per-neuron partition above sums single-cell informations, but a
redundant population code carries far less than that sum. To test this
we estimate, for each family and each cell type, the \emph{joint} MI of
the group's activity about heading and about displacement --- a
cross-validated multinomial-logistic decoder lower bound
(\cref{eq:zhd_mi_joint_lb}) --- and compare it to the summed per-neuron
MI (\cref{fig:zhd_mi_grouped}). The two diverge sharply: while the
summed per-neuron MI runs to tens or hundreds of bits, the group joint
MI saturates near the heading-entropy ceiling ($\log_2 n_\theta$ bits)
for every family. The recurrent code is therefore strongly
\emph{redundant} --- adding neurons buys little extra information once a
group already reads out the variable --- rather than additive. The
saturation is tighter and reached at smaller groups for heading than
for displacement, and the displacement MI is markedly weaker in the
leaky 2-D model, mirroring the per-neuron picture: heading is the more
robustly and redundantly encoded of the two integrated variables.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_zebrafish_mi_grouped.png}
  \caption{\textbf{Group joint MI versus summed per-neuron MI: a
  redundant recurrent code.} For each anatomical group --- the four
  families (diamonds: \emph{dIPN} amber, \emph{dsIPN} teal, \emph{IPN12}
  rose, \emph{IPN-core} purple) and each fish2 recurrent cell type
  (small dots, coloured by family) --- the group's \emph{joint} mutual
  information about the target (cross-validated multinomial-logistic
  decoder lower bound, \cref{eq:zhd_mi_joint_lb}) is plotted against the
  sum of its neurons' per-neuron MI (logarithmic $x$-axis). Columns,
  left to right: heading\,$+$\,distance (cumulative),
  heading\,$+$\,distance (leaky), 2-D path (cumulative), 2-D path
  (leaky). \textbf{(a--d)}~heading $\theta$; \textbf{(e--h)}~forward
  displacement $d$. Axis titles are written once, on panel \textbf{a}.}
  \label{fig:zhd_mi_grouped}
\end{figure}

\subsection{Task ability does not imply realistic activity}
\label{sec:results_obs_baseline}

The results so far establish that the connectome-constrained network learns
to integrate heading accurately, and that the solution is robust to any
single-type lesion. Accuracy on the task, however, says nothing about
whether the network's \emph{internal activity} matches the brain. We test
this directly by driving the trained task-only model
(\emph{zebrafish\_hd\_si\_ipn\_917\_v1}, the nominal $917$-cell circuit,
trained on the rotation task \emph{only}) with the recorded ZAPBench
``Rotations'' stimulus (\cref{method:zapbench}), converting its
rolled-out activity to GCaMP7f calcium (\cref{eq:zhd_gcamp}), and
comparing it to the recorded $\Delta F/F$ of the $279$ matched ring-pool
neurons in the same rastermap row order (\cref{fig:zhd_obs_baseline}).

The model is an excellent head-direction predictor --- its decoded heading
tracks the $45^\circ/\mathrm{s}$ rotation closely
(\cref{fig:zhd_obs_baseline}, bottom panel) --- yet its population activity is
plainly \emph{not} the recorded one: the model's learned kinograph is a
clean, stimulus-locked set of bands, whereas the recorded $\Delta F/F$
above it is irregular, bursty and drift-laden. This
is the degeneracy at the heart of the paper, now read out on real data:
\emph{many circuits realise the same head-direction computation, and hence
the same task ability, while producing different activity}. Task
supervision constrains only the two-dimensional readout
(\cref{eq:zhd_readout}); the $N$-dimensional state it leaves unobserved is
exactly where implementations diverge.

A natural objection is that the mismatch is merely cosmetic: perhaps the
recorded activity still encodes heading, just in a form our eye does not
read off the kinograph. We tested this directly by training a decoder ---
a small neural network --- to predict the instantaneous heading
$(\cos\theta,\sin\theta)$ from the recorded per-frame $\Delta F/F$ of the
ring-pool neurons (fit on the first $70\%$ of the rotation block,
evaluated on the held-out tail). The result was \emph{not} conclusive: the
decoded heading does not track the true heading
(\cref{fig:zhd_obs_control}; recorded-$\Delta F/F$ decode at circular
$r=0.14$), in sharp contrast to the model's own readout, which
recovers heading almost perfectly from its latent state
(\cref{fig:zhd_obs_baseline}, bottom panel). The implication is that \emph{the
instantaneous head-direction signal is hard to read, frame by frame, from
the observed calcium activity}.

How much of this failure is the \emph{observable} rather than the brain? We
settle the confound with a control: run the \emph{identical} decoder on the
trained model pushed through the \emph{same} observation pipeline --- its
rolled-out activity convolved with the GCaMP kernel, sampled at the
$1.09\,\mathrm{Hz}$ imaging rate and restricted to the same $300$ cells
(\cref{fig:zhd_obs_control}). The model provably encodes heading: its latent
readout decodes the heading at circular $r = 0.97$. Through the degraded
observable the \emph{same} circuit decodes at only $r = 0.73$ --- the
observation pipeline alone (GCaMP low-pass of the fast $45^\circ/\mathrm{s}$
ring activity, $\tau_d = 1.2\,\mathrm{s}$, \cref{eq:zhd_gcamp}; $1.09\,\mathrm{Hz}$
aliasing of an $8\,\mathrm{s}$ revolution; only $300$ of $839$ cells seen)
removes a substantial fraction of the per-frame legibility. It does
\emph{not}, however, reach the real-data level ($r = 0.14$): a circuit that
encoded heading as cleanly as the model would still be decodable at
$r \approx 0.73$ under the recording's observation, so the observable is a
major but \emph{not} the sole cause. The residual gap reflects additional
real-world degradation --- recording noise, behavioural variability of the
animal relative to the grating, and the idealised, stimulus-locked model
ring activity being crisper than the brain's. Either way heading is carried in the
population's \emph{temporal dynamics}, not in any single calcium frame ---
which is why the observation loss matches the time-course of each neuron's
trace (\cref{eq:zhd_obs}, a per-neuron shape loss) rather than asking the
activity to decode heading frame-by-frame.

This motivates the calcium observation loss (\cref{eq:zhd_obs}): by adding
a term that pulls the model's predicted calcium toward the recorded
$\Delta F/F$, we aim to \emph{partially break} this degeneracy ---
selecting, among the task-equivalent circuits, those whose activity is
also consistent with the measured brain. Runs that train with the
observation loss are reported in \cref{sec:results_obs_sweep};
\cref{fig:zhd_obs_baseline} is the no-observation-loss
($\lambda_{\mathrm{obs}} = 0$) baseline against which they are compared.

\begin{figure}[p]
  \centering
  \includegraphics[width=0.7\textwidth]{\figdir/fig_zebrafish_calcium_baseline.png}
  \caption{\textbf{Task-only model: accurate heading, unrealistic activity.}
  The nominal $917$-cell model
  (\emph{zebrafish\_hd\_si\_ipn\_917\_v1}, heading task only, no
  observation loss) driven by the recorded ZAPBench Rotations stimulus
  and rendered as GCaMP7f calcium (\cref{eq:zhd_gcamp}), in the same
  reconstruction layout as \cref{fig:zhd_obs_reconstruction}. Top:
  recorded angular-velocity drive $\omega(t)$. For the bump-pool
  ($n=279$ matched ring-pool neurons, official ZAPBench rastermap row
  order) and the \emph{ARTR} afferent class ($n=33$, sorted by per-cell
  preferred-heading angle, \cref{fig:zhd_unmodeled}), two rows are
  shown: the recorded $\Delta F/F$ and the model's calcium under a
  continuous $600\,\mathrm{s}$ rollout, with per-panel $z$-MSE and SSIM
  inset; each row is the same recorded cell across the recorded and
  model panels. Bottom: true heading (green) and the model's
  readout-decoded heading (black) over the same rollout. The model
  decodes heading accurately, yet its predicted calcium is clean,
  stimulus-locked banding bearing little
  resemblance to the irregular, drift-laden recording --- the
  degeneracy the observation loss (\cref{eq:zhd_obs};
  \cref{sec:results_obs_sweep}) targets. pt-IPN1 and motor\_efferent are
  omitted here (shown with the observation loss on in
  \cref{fig:zhd_obs_reconstruction}).}
  \label{fig:zhd_obs_baseline}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_zebrafish_obs_control.png}
  \caption{\textbf{Observability control: the same per-frame decoder applied
  to the model's matched observable.} The task-only model is driven by the
  ZAPBench Rotations stimulus and its decoded heading read three ways over
  the block (circular correlation $r$ on the held-out tail).
  \textbf{Left:} true heading (green) vs.\ the model's latent readout (black);
  the MLP decode of the model's GCaMP-convolved,
  imaging-sampled, $300$-cell observable (red); and the MLP decode
  of the recorded $\Delta F/F$ (blue); the time axis is shown for
  the first $300\,\mathrm{s}$ for legibility (the $r$ values are computed over
  the full held-out tail). \textbf{Right:} the three held-out correlations.
  Pushing a circuit that provably encodes heading
  through the recording's observation pipeline drops the per-frame decode
  well below the model's own latent readout but not to the real-data
  level, so the observable is a substantial but not sole cause of the
  per-frame decode failure.}
  \label{fig:zhd_obs_control}
\end{figure}

\subsection{Signals not yet accounted for: the fish's own behaviour}
\label{sec:results_unmodeled}

The task-only model's calcium is essentially uncorrelated with the
recording (\cref{sec:results_obs_baseline}; \cref{fig:zhd_obs_baseline}),
and the ceiling on what any model can recover here is set in part by
what the model is \emph{allowed to see}. The only time-varying input
to the network is the angular-velocity drive $s(t)$, which for the
Rotations block is an essentially periodic $\pm 45^\circ/\mathrm{s}$
square wave. A deterministic circuit driven by a periodic input can
only produce (after transients) a periodic output, so \emph{any}
non-periodic structure in the recorded $\Delta F/F$ is, by construction,
outside the model's reach --- and the recorded ring activity is
conspicuously non-periodic across the $600\,\mathrm{s}$ block even
though the stimulus repeats.

\Cref{fig:zhd_unmodeled} lays the recording's covariates side by side
over the rotation block to make the missing signals explicit. Above
the recorded $\Delta F/F$ we plot, from the raw ZAPBench ephys, the
fish's own swimming behaviour --- the quantity the model never
receives. The two bottom traces are the left and right tail-muscle
EMG channels (the independent motor readout); the larval-zebrafish
swim decode summarises them as a \emph{forward} drive (bilateral
envelope) and a \emph{turning} signal (left/right asymmetry). These
behave very differently with respect to the stimulus. Turning is
largely stimulus-locked --- it correlates with the drive at
$\rho \approx 0.76$, the expected optomotor following of the grating
--- and so a stimulus-driven model can in principle capture it.
Forward drive, by contrast, is a slow, non-periodic latent: its
lag-one autocorrelation is $\approx 0.97$ and it is essentially
uncorrelated with the stimulus magnitude ($|\rho| \lesssim 0.05$). It
drifts on the tens-of-seconds-to-minutes timescale that dominates the
recurrent pool's irregular activity, yet nothing in $s(t)$ carries
it.\footnote{The forward/turning traces are derived from the two
tail-EMG electrodes by the standard ZAPBench swim-EMG pipeline
(windowed-variance envelope, swim-bout detection, per-bout weighting);
we verified they are genuinely those channels by recovering
\emph{forward} from the bilateral EMG sum ($\rho \approx 0.57$) and
\emph{turning} from the left$-$right difference ($\rho \approx 0.42$).
The per-frame RMS shown here is a coarse envelope of the same
channels.}

Three classes of signal are therefore present in the recording but
absent from the current model. (i) \emph{Self-generated motor drive}
--- the forward/turning behaviour above, which is internally generated
and not a deterministic function of the visual stimulus; the forward
component in particular injects exactly the slow, aperiodic variation
the model cannot synthesise. (ii) \emph{Slow physiological and state
drift} --- bleaching, arousal, and adaptation accumulate over the
block, which is why per-trial $z$-scoring is necessary in
\cref{eq:zhd_obs} to remove this component rather than fit it.
(iii) \emph{Unmodelled sensory channels} --- the recording also
carries the raw visual-velocity and other stimulus channels at full
temporal resolution, of which the model receives only the distilled
heading drive. None of these are failures of identifiability: they
are inputs the network is simply not given. With this in mind we now
turn the observation loss on (\cref{sec:results_obs_sweep}) and see
how far it can close the gap.

\paragraph{Overall: do the results agree with the introduction?}
The introductory picture is a ring-attractor head-direction circuit ---
hundreds of GABAergic \rpi{} cells in the anterior hindbrain that carry a
single sinusoidal activity peak travelling around the functional ring,
persisting for tens of seconds, and tracking the swum heading, supported
by the reciprocal dIPN connectivity the EM reconstruction reveals
\citep{petrucco2023}. Our trained, connectome-locked circuit reproduces
the computational core of this picture: it integrates heading at
near-unit gain and carries a single travelling activity peak on the dIPN
ring (\cref{sec:results_evolution}), realised on the measured fish2
wiring with the magnitudes sitting on the signed connectome support. In
that sense the results agree with the introduction --- the ring-attractor
account is realisable on the real connectome. They also sharpen its open
questions in two ways the introduction did not anticipate. First, the
implementation is \emph{degenerate}: many connectome-consistent circuits
integrate equally well, the preferred-heading map is not anatomically
organised, and the recurrent pool mixes heading and translation rather
than segregating them by cell type (\cref{sec:results_four_classes}) ---
so anatomy fixes \emph{that} the circuit integrates, not \emph{how} the
code is laid out. Second, the introductory ring activity is only
\emph{partly} matched on the recorded data: the task-only model's calcium
is nearly uncorrelated with the recording until the observation loss is
added (\cref{sec:results_obs_sweep}), and the recorded ARTR carries an
intrinsic dark-state rhythm that the command-driven model lacks
(\cref{sec:results_artr}). The agreement is therefore at the level of the
\emph{computation} the introduction describes, while the
\emph{implementation} and the \emph{full recorded dynamics} remain
under-determined --- the gap the rest of the paper sets out to measure.

\begin{figure}[p]
  \centering
  \includegraphics[width=0.8\textwidth]{\figdir/fig_zebrafish_rotation_covariates.png}
  \caption{\textbf{Signals present in the recording but not given to
  the model, over the ZAPBench Rotations block} ($656$ frames
  $\approx 600\,\mathrm{s}$; shared time axis). Top to bottom: the
  periodic angular-velocity drive $s(t)$ (the model's only input),
  its integrated heading, the stimulus direction; the larval-fish
  \emph{forward} (slow, $\sim$stimulus-independent drift) and
  \emph{turning} (largely stimulus-locked with $s(t)$) swim decodes; the raw left/right tail-EMG channels (RMS)
  the decodes are computed from; the recorded $\Delta F/F$ of the three
  velocity-gate afferent classes in the \cref{fig:zhd_3d} colour code ---
  \emph{ARTR} (blue, angular drive), \emph{pt-IPN1} (orange, exteroceptive
  forward) and \emph{motor\_efferent} (green, angular efference),
  matched cells only; and two views of the recorded
  $n{=}300$ ring-pool $\Delta F/F$ --- the global ZAPBench rastermap
  permutation (matches \cref{fig:zhd_obs_baseline}) and a per-neuron
  preferred-heading-angle sort computed from the same calcium. The ARTR
  and motor\_efferent afferents show clear rotation-locked banding (they
  carry the drive), whereas the
  aperiodic structure visible in both ring-pool kinographs tracks the
  behavioural drive but has no counterpart in the periodic stimulus,
  bounding the calcium reconstruction of \cref{sec:results_obs_sweep}
  from above.}
  \label{fig:zhd_unmodeled}
\end{figure}

\subsection{Calcium-observation loss partially closes the activity gap}
\label{sec:results_obs_sweep}

We now turn the observation loss on. Using the sign-locked RNN of
\cref{method:RNN} (the same recurrent dynamics used throughout
Section 4) we trained the $839$-cell \emph{IPN12 precursor} circuit
(\cref{method:zapbench}; the connectome before the IPN-core families
were added to reach the nominal $917$-cell circuit) on the
swim-integration task (dataset~A) while augmenting each minibatch with
$B_c$ recorded ``Rotations'' trials (dataset~B) and matching predicted
to recorded calcium with the $z$-scored shape loss of \cref{eq:zhd_obs},
sweeping the observation weight
$\lambda_{\mathrm{obs}} \in \{0,\,0.1,\,0.5,\,1,\,5\}$
($\lambda_{\mathrm{obs}}=0$ is the task-only control). Each trained model is then driven by the
held-out recorded stimulus and its rolled-out activity rendered to
GCaMP7f calcium exactly as for the baseline (\cref{fig:zhd_obs_baseline}),
and scored against the recorded $\Delta F/F$ over a continuous
$600\,\mathrm{s}$ rollout (\cref{tab:zhd_obs_sweep},
\cref{fig:zhd_obs_reconstruction}). The figure of merit is the per-neuron
temporal $z$-MSE of \cref{eq:zhd_obs}, equal to $2(1-\rho)$ with $\rho$
the predicted-vs-recorded Pearson correlation, reported separately for
the $300$ ring-pool cells and the $181$ afferents.

The loss does what it is designed to. With $\lambda_{\mathrm{obs}} = 0$
the task-only model's calcium is essentially uncorrelated with the
recording ($z$-MSE $\approx 1.88$ for both pools, $\rho \approx 0.06$):
the network integrates heading perfectly yet its activity is unrelated to
the brain's, exactly the degeneracy of \cref{sec:results_obs_baseline}.
Turning the loss on collapses this error immediately --- already at the
smallest weight $\lambda_{\mathrm{obs}} = 0.1$ the ring-pool $z$-MSE
drops to $1.25$ ($\rho \approx 0.38$) and the afferent pool to $0.92$
($\rho \approx 0.54$) --- and larger weights buy little further reduction
($z$-MSE plateaus at $1.1$--$1.3$ for the ring activity peak and $0.90$--$0.98$ for the
afferents across $\lambda_{\mathrm{obs}} \in [0.1, 5]$). Most of the
benefit is therefore captured by the mere presence of the term rather
than its magnitude. The afferent (\emph{RIPN}/\emph{pt-IPN}) pool is
reconstructed markedly better than the recurrent pool (afferent
$\rho \approx 0.54$ and SSIM up to $0.44$, vs.\ ring-pool $\rho \approx 0.40$
and SSIM $\approx 0.18$), consistent with the afferents carrying the
stimulus-locked part of the recording while the ring activity peak is the irregular,
drift-laden activity that is hardest to match.

The fit is partial, not complete, and trades against the task at large
weights. Even at its best the ring-pool correlation plateaus near
$\rho \approx 0.4$: the loss pulls the predicted activity toward the
recording but does not reproduce it, so the degeneracy of
\cref{sec:results_obs_baseline} is \emph{partially}, not fully, broken
--- the expected outcome for a single per-neuron shape loss applied to
$481$ of the $839$ cells. Pushing $\lambda_{\mathrm{obs}}$ higher does not
help and begins to hurt: the structural SSIM peaks at the smallest weight
($\lambda_{\mathrm{obs}} = 0.1$) and declines thereafter, and the
decoded-heading panels (\cref{fig:zhd_obs_reconstruction}, bottom)
develop more frequent slips as the observation term competes with the
heading MSE. An intermediate weight
($\lambda_{\mathrm{obs}} \approx 0.1$--$0.5$) is thus the operating point
that best balances calcium realism against task accuracy.

\begin{table}[H]
\centering
\small
\begin{tabular}{c c c c c}
\toprule
$\lambda_{\mathrm{obs}}$ & ring-pool $z$-MSE & ring-pool SSIM
       & aff.\ $z$-MSE & aff.\ SSIM \\
\midrule
$0$   & $1.88$ & $\mathbf{0.29}$ & $1.83$ & $0.21$ \\
$0.1$ & $1.25$ & $0.17$ & $0.92$ & $\mathbf{0.44}$ \\
$0.5$ & $\mathbf{1.14}$ & $0.18$ & $0.98$ & $0.43$ \\
$1.0$ & $1.31$ & $0.16$ & $0.95$ & $0.37$ \\
$5.0$ & $1.21$ & $0.17$ & $\mathbf{0.90}$ & $0.22$ \\
\bottomrule
\end{tabular}
\caption{\textbf{Calcium-reconstruction quality vs.\ observation weight
$\lambda_{\mathrm{obs}}$.} GNN model, $839$-cell IPN12 circuit, scored on
the held-out continuous $600\,\mathrm{s}$ rollout against the recorded
ZAPBench Rotations block. $z$-MSE is the per-neuron temporal loss of
\cref{eq:zhd_obs} (lower is better; $z\text{-MSE} = 2(1-\rho)$ with $\rho$
the predicted-vs-recorded Pearson correlation);
SSIM is the structural-similarity index of the kinograph (higher is
better). Bold marks the best entry in each column.}
\label{tab:zhd_obs_sweep}
\end{table}

\paragraph{Task generalisation across the gcamp family.}
The observation-loss recipe is task-agnostic --- the per-neuron
$z$-scored shape loss does not refer to heading, distance, or any
other behavioural variable --- so the same training procedure can
be swapped onto the other swim-integration sub-tasks of
\cref{sec:results_variants}. We train two such companions of
\cref{fig:zhd_obs_reconstruction}, both supervised against the
recorded \emph{Rotations} block of dataset~B: (i) the joint
heading-plus-distance task (\cref{fig:zhd_obs_reconstruction_both},
the ``selfmotion\_both'' four-input variant) and (ii) the
translation-only task
(\cref{fig:zhd_obs_reconstruction_translation},
``selfmotion\_translation''). For the four-input model the
continuous test-time rollout is now driven jointly by the recorded
$\omega(t)$ and the recorded swim-forward envelope
$v_{\mathrm{fwd}}(t)$ (read from the calcium dataset's
swim-forward channel, the same channel the trainer feeds into the
calcium-batch trials) --- i.e.\ the model sees at test exactly the
four channels it was trained on. For the translation-only model
the rollout is driven by $v_{\mathrm{fwd}}$ alone. The pattern is
consistent across these two variants: the afferent pool
reconstructs at $\rho \approx 0.4$ (SSIM $0.39$--$0.48$), the
recurrent ring-pool at $\rho \approx 0.2$--$0.3$ (SSIM
$0.20$--$0.25$), and the per-frame task readouts (heading and/or
distance) remain accurately decoded in every case; the observation
loss does not collapse onto the task heads. The Turning block of
dataset~B is excluded from this comparison because its recorded
stimulus is stored as discrete L/R direction codes (no angular
velocity in the dataset's first channel), which leaves the input
gate driving the trained heading-only model with zeros and
therefore cannot supervise the heading-driven activity meaningfully.

\begin{figure}[p]
  \centering
  \includegraphics[width=0.8\textwidth]{\figdir/fig_zebrafish_calcium_reconstruction.png}
  \caption{\textbf{Calcium reconstruction with the observation loss on
  ($\lambda_{\mathrm{obs}} = 1$, heading task).} The
  sign-locked RNN of \cref{method:RNN}, trained on the
  heading-only task ($n_{\mathrm{in}} = 3$, $n_{\mathrm{out}} = 2$,
  target $(\cos\theta, \sin\theta)$) with calcium-observation
  supervision, is driven by the held-out recorded ZAPBench Rotations
  stimulus (top: angular-velocity drive $\omega(t)$) and its
  rolled-out activity rendered to GCaMP7f calcium
  (\cref{eq:zhd_gcamp}). For the recurrent pool ($300$ cells, rastermap
  row order) and each of the three afferent classes (\emph{ARTR},
  \emph{pt-IPN1}, \emph{motor\_efferent}), two rows are
  shown: the recorded $\Delta F/F$ and the model's calcium under a
  continuous $600\,\mathrm{s}$ rollout; per-panel $z$-MSE and SSIM are
  inset. Each afferent class is sorted by per-cell preferred-heading
  angle (\cref{fig:zhd_unmodeled}), the same order in its recorded and
  model rows. Bottom: true heading
  (green) and the model's readout-decoded heading (black) over the
  same continuous rollout. Relative to the task-only baseline
  (\cref{fig:zhd_obs_baseline}) the predicted calcium is now visibly
  banded in register with the recording --- most clearly for the
  stimulus-locked afferents --- while heading remains accurately
  decoded. \Cref{tab:zhd_obs_sweep} reports the full
  $\lambda_{\mathrm{obs}}$ sweep.}
  \label{fig:zhd_obs_reconstruction}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.8\textwidth]{\figdir/fig_zebrafish_calcium_reconstruction_both_gcamp.png}
  \caption{\textbf{Calcium reconstruction --- joint
  heading-plus-forward-distance task} ($\lambda_{\mathrm{obs}} =
  1$). Same layout as \cref{fig:zhd_obs_reconstruction} for the
  joint heading $+$ distance variant
  (``selfmotion\_both''): $n_{\mathrm{in}} = 4$, $n_{\mathrm{out}}
  = 3$, target $(\cos\theta, \sin\theta, d)$ with $d$ the
  \emph{cumulative} forward distance $d(t)=\int v_{\mathrm{fwd}}\,ds$
  (no leak --- the parent ``selfmotion\_both'' config has no
  $\xi$-decay time constant). Both drive channels the trained
  $4$-input model expects are now plotted at the top: $\omega(t)$
  is the recorded ZAPBench Rotations angular-velocity drive, and
  $v_{\mathrm{fwd}}(t)$ is the recorded swim-forward envelope read
  from the calcium dataset's $\mathrm{swim\_forward.zarr}$ (the
  same tail-EMG channel the trainer feeds into the calcium-batch
  trials, \cref{fig:zhd_unmodeled}); the continuous rollout below
  is driven by both. The bottom panel shows the model's decoded
  $d$ (black) against the cumulative reference
  $\int v_{\mathrm{fwd}}\,ds$ (green) over the continuous
  $600\,\mathrm{s}$ block.}
  \label{fig:zhd_obs_reconstruction_both}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.8\textwidth]{\figdir/fig_zebrafish_calcium_reconstruction_translation_gcamp.png}
  \caption{\textbf{Calcium reconstruction --- translation-only task}
  ($\lambda_{\mathrm{obs}} = 1$). Translation-only variant
  (``selfmotion\_translation''): $n_{\mathrm{in}} = 1$,
  $n_{\mathrm{out}} = 1$, target $d(t) = \int v_{\mathrm{fwd}}\,ds$
  (cumulative). The drive at the top is the recorded swim-forward
  envelope $v_{\mathrm{fwd}}(t)$ (the only channel this $1$-input
  model receives at test time); the $\omega$ panel of
  \cref{fig:zhd_obs_reconstruction_both} is absent here because the
  model has no rotation input. Below: bump-pool and afferent real /
  continuous-rollout kinographs in matched rastermap order, per-pool
  $z$-MSE / SSIM inset. Bottom: decoded forward distance $d$ (black)
  against the cumulative reference $\int v_{\mathrm{fwd}}\,ds$
  (green) over the continuous $600\,\mathrm{s}$ block; no heading
  panel since heading is not decoded in this variant.}
  \label{fig:zhd_obs_reconstruction_translation}
\end{figure}

\subsection{Proprioceptive 2-D path integration under calcium supervision}
\label{sec:results_propriocep}

The proprioception-extended circuit of \cref{method:proprioception}
(\cref{fig:zhd_3d}\textbf{d}) routes the forward-swim drive
$v_{\mathrm{fwd}}$ to IPN through two anatomically distinct ports in
parallel --- the exteroceptive \emph{pt-IPN1} channel and the
motor-efferent \emph{RIPN11/12\_a/12\_c} channel --- and is trained on
the full 2-D path-integration task ($n_{\mathrm{in}}=5$,
$n_{\mathrm{out}}=4$, target $(\cos\theta,\sin\theta,x,y)$) \emph{with}
the calcium-observation loss on (dataset~B ``Rotations'',
$\lambda_{\mathrm{obs}}=1$, $B_c=8$). This is the single circuit in
which the proprioceptive afferent split, the hardest (2-D path) target,
and the real-calcium supervision are all active at once.
The proprioceptive circuit differs from the task-only 2-D models
(\cref{fig:zhd_evolution_position2d,fig:zhd_evolution_position2d_leaky})
only in the second (proprioceptive) afferent port and the added
observation term. Heading is still recovered on held-out swim trials (per-trial
Pearson $r_\theta \approx 0.94$), and the 2-D-position head integrates
the path at a per-frame Euclidean RMSE of $\approx 0.38$ on natural
trials for the cumulative variant --- the calcium term and the extra
afferent port do not degrade the task.

Driving these same two checkpoints with the recorded ZAPBench Rotations
stimulus and rendering the rolled-out activity to GCaMP7f calcium
(\cref{eq:zhd_gcamp}) gives the reconstructions of
\cref{fig:zhd_obs_reconstruction_propriocep_position2d,fig:zhd_obs_reconstruction_propriocep_position2d_leaky}.
The layout follows \cref{fig:zhd_obs_reconstruction_translation}, except
that the bottom panel shows the decoded 2-D \emph{trajectory}
$(\hat x,\hat y)$ against the cumulative path-integration reference, in
spatial coordinates, rather than the scalar forward-distance trace. As
elsewhere the afferent pool reconstructs markedly better than the
recurrent ring pool (cumulative: afferent $z$-MSE $=0.97$, SSIM $=0.43$
vs.\ ring-pool $z$-MSE $=1.12$, SSIM $=0.17$; leaky: afferent
$z$-MSE $=0.94$, SSIM $=0.59$ vs.\ ring-pool $z$-MSE $=1.04$,
SSIM $=0.20$), consistent with the afferents carrying the
stimulus-locked part of the recording. The decoded trajectory makes the
leaky-vs-cumulative contrast explicit on the continuous $600\,\mathrm{s}$
rollout: the cumulative decode drifts away from the origin while the
leaky decode stays bounded near it, the spatial analogue of the scalar
forward-distance trace in \cref{fig:zhd_obs_reconstruction_translation}.

\begin{figure}[p]
  \centering
  \includegraphics[width=0.8\textwidth]{\figdir/fig_zebrafish_calcium_reconstruction_propriocep_position_2d_gcamp.png}
  \caption{\textbf{Calcium reconstruction --- proprioceptive 2-D
  path-integration task, cumulative target} ($\lambda_{\mathrm{obs}} =
  1$). Same layout as \cref{fig:zhd_obs_reconstruction_translation} for
  the proprioception-extended circuit ($n_{\mathrm{in}}=5$,
  $n_{\mathrm{out}}=4$, target $(\cos\theta,\sin\theta,x,y)$). Top: the
  recorded ZAPBench Rotations angular-velocity drive $\omega(t)$ and the
  recorded swim-forward envelope $v_{\mathrm{fwd}}(t)$. For the
  ring pool ($300$ cells, rastermap order) and each of the three
  afferent classes (\emph{ARTR}, \emph{pt-IPN1}, \emph{motor\_efferent},
  sorted by per-cell preferred-heading angle), the recorded $\Delta F/F$
  and the model's GCaMP7f calcium under a continuous
  $600\,\mathrm{s}$ rollout are shown, with per-panel $z$-MSE and SSIM
  inset. \emph{Bottom panel
  (replacing the scalar-$d$ trace of
  \cref{fig:zhd_obs_reconstruction_translation}):} the decoded 2-D path
  $(\hat x,\hat y)$ (black) against the cumulative path-integration
  reference $(\,\int v_{\mathrm{fwd}}\cos\theta,\ \int
  v_{\mathrm{fwd}}\sin\theta\,)$ (green) in spatial coordinates over the
  same continuous block; the marker is the shared origin. The cumulative
  decode drifts from the origin as the unbounded reference grows.}
  \label{fig:zhd_obs_reconstruction_propriocep_position2d}
\end{figure}

\begin{figure}[p]
  \centering
  \includegraphics[width=0.8\textwidth]{\figdir/fig_zebrafish_calcium_reconstruction_propriocep_position_2d_leaky_gcamp.png}
  \caption{\textbf{Calcium reconstruction --- proprioceptive 2-D
  path-integration task, leaky target} ($\lambda_{\mathrm{obs}} = 1$,
  $\tau_p = 0.5\,$s). Same layout as
  \cref{fig:zhd_obs_reconstruction_propriocep_position2d}. In the bottom
  trajectory panel the decoded path (black) stays bounded near the
  origin --- the signature of the leaky 2-D integrator
  (\cref{eq:xy_pi}) --- while the cumulative reference (green) drifts,
  the spatial counterpart of the bounded-vs-growing distance in
  \cref{fig:zhd_obs_reconstruction_translation}.}
  \label{fig:zhd_obs_reconstruction_propriocep_position2d_leaky}
\end{figure}

\section{Outlook: one circuit, many tasks across the ZAPBench conditions}
\label{sec:outlook}

\paragraph{The idea, in plain terms.}
The Rotations block is one of \emph{nine} conditions in the same continuous
recording of the same $481$ neurons (\cref{fig:zhd_allblocks}); we have so far
used only the $600\,\mathrm{s}$ rotation slice. The contradiction of
\cref{sec:results_obs_baseline} --- the model decodes heading from its readout
at $\rho = 0.97$, yet heading cannot be read frame-by-frame from the recorded
activity ($\rho = 0.14$, \cref{fig:zhd_obs_control}) --- only arises if one
assumes heading must live in the \emph{raw} per-neuron activity. It need not.
If each cell carries a \emph{mixture} of two variables --- heading
$\theta(t)$ and a second, behaviourally meaningful variable $d(t)$ that is
roughly orthogonal to heading in population space --- then a \emph{tuned}
readout that projects onto the heading subspace recovers $\theta$ cleanly
(as the model's own readout does), while a \emph{naive} per-frame decoder,
seeing $d$ as nuisance variance, cannot. Two signals share one population
the way two radio stations share one wire: with the right filter each is
legible, without it the sum is garbled. Crucially, if $d$ is itself slow and
aperiodic, mixing it in makes the population activity aperiodic --- exactly the
recorded signature the heading-only model cannot produce
(\cref{sec:results_unmodeled}). The second variable thus resolves the
task/observation tension instead of trading it off: it is the structured
version of simply injecting noise.

\paragraph{Mask or mixed selectivity --- a testable distinction.}
The orthogonality that makes both variables recoverable lives in the readout
\emph{weights}, not necessarily in a partition of the cells, and it can be
realised two ways. In the \emph{labelled-line} case the variables occupy
disjoint cell sets --- one subpopulation codes heading, the complementary one
codes $d$ --- and orthogonality is automatic because the readouts share no
cells. In the \emph{mixed-selectivity} case the \emph{same} cells carry both
and the two readouts are orthogonal as weight \emph{vectors} while overlapping
in support: for two neurons, $w_\theta=(1,1)$ and $w_d=(1,-1)$ give
$w_\theta\!\cdot\!w_d=0$ yet every cell contributes to both. The disjoint
mask is the special, sparse case; mixed selectivity --- the general one --- is
what most heading and cortical populations are observed to do. Which holds here
is an empirical question the recording answers directly, with no model: score
each recurrent cell's tuning to heading (its per-neuron information,
\cref{eq:zhd_mi_plugin}) against its forward-swim weight (the regression of
\cref{fig:zhd_blockenc}). If the heading-tuned and forward-tuned cells are
largely \emph{disjoint}, the second variable is a separate sub-population (a
mask); if many single cells score on \emph{both}, it is genuinely multiplexed
onto the heading cells --- the stronger claim, and the one that would make the
dIPN a mixed heading/self-motion code rather than two interleaved
labelled lines. This single per-cell tuning scatter is the cleanest near-term
test of the two-variable hypothesis.

\paragraph{A multi-task objective.}
This is a small generalisation of the model we already have. The single
heading readout (\cref{eq:zhd_readout}) becomes a set of per-variable heads
sharing the same recurrent state,
\begin{equation}
\hat y^{(k)}(t) = \hat W^{(k),\mathrm{out}}\,\sigma\!\big(\hat h(t)\big)
  + \hat b^{(k),\mathrm{out}},
\qquad k \in \{\theta,\ d,\ \dots\},
\label{eq:zhd_multihead}
\end{equation}
where $k=\theta$ is the heading head of \cref{eq:zhd_readout} and $k=d$ is a
\emph{self-motion} head supervised by a leaky accumulator of the measured
forward-swim drive $u^{\mathrm{fwd}}$ (the slow, $\sim$stimulus-independent
behaviour of \cref{sec:results_unmodeled}),
\begin{equation}
d(t) = \int_0^{t} e^{-(t-s)/\tau_d}\, u^{\mathrm{fwd}}(s)\,ds
\qquad (\text{``how far the fish has swum recently''}),
\label{eq:zhd_selfmotion}
\end{equation}
an aperiodic quantity the heading drive does not carry. Training then runs
over all blocks at once, with a mask $m^{(k)}_b\in\{0,1\}$ that turns on each
task only where its variable is actually driven, while the calcium-observation
loss (\cref{eq:zhd_obs}) runs on \emph{every} block,
\begin{equation}
\mathcal{L} = \sum_{b\in\mathcal{B}}
  \Big[\ \sum_k m^{(k)}_b\,\lambda_k\,
        \mathcal{L}^{(k)}_{\mathrm{task},b}
      \ +\ \lambda_{\mathrm{obs}}\,\mathcal{L}_{\mathrm{obs},b}\ \Big].
\label{eq:zhd_multitask}
\end{equation}
Each block supplies supervision for whatever it excites; the shared,
connectome-locked circuit must represent all of it at once. Setting
$\mathcal{B}=\{\text{Rotations}\}$ and $k=\{\theta\}$ recovers the present
model exactly.

\paragraph{Why this is a measurement, not a free latent.}
The second target $d$ is computed from an \emph{independent} signal (the tail
EMG), not inferred from the calcium it must explain --- so it cannot absorb
arbitrary structure the way an unconstrained latent input would. Two
conditions keep it honest, and both are properties of the \emph{data}, not the
optimiser: the second head must (i) generalise to a block it was not fit on,
and (ii) beat a phase-scrambled surrogate of $d$. There is also a subtler
requirement --- $\theta$ and $d$ must be \emph{decorrelated} in training, or
the two heads collapse onto a single entangled axis instead of two orthogonal
codes. A single block cannot guarantee this (in Rotations, turning is locked
to the grating); the \emph{union} of blocks does --- Gain~/~OMR drive forward
swimming while heading is not rotating, Rotations drives heading --- so the
conditions together dissociate the variables. This decorrelation question is
ultimately about biology: because the circuit is locked to the real
connectome, whether it \emph{can} hold two orthogonal codes given dissociating
input is a falsifiable test of whether the fish's wiring can multiplex at all.

\paragraph{Which blocks are worth wiring in.}
Before any retraining, the per-block encoding diagnostic
(\cref{fig:zhd_blockenc}; the all-blocks analogue of \cref{fig:zhd_obs_control})
regresses the recorded ring-pool $\Delta F/F$ on the signals available in every
block --- the stimulus code and the forward/turning swim --- to show where the
activity is explainable and where forward-swim carries variance the stimulus
does not. Three facts stand out. (i)~\textbf{OMR~Turning is the keystone}: the
recurrent pool is most explainable there ($R^2 = 0.40$, largely stimulus-driven), it
co-drives heading, turning \emph{and} forward at the highest calcium activity,
and it is the longest block ($1200\,\mathrm{s}$). (ii)~\textbf{Forward-swim adds
significant, surrogate-beating variance} in Rotation ($\Delta R^2 \approx
0.09$), Flash ($0.10$), Taxis ($0.08$) and Turning ($0.05$) --- the
second-variable signal is present across conditions, not an artefact of one
block. (iii)~\textbf{Several blocks are near-unexplainable} from the measured
covariates (Gain, Position, Open-Loop and Darkness sit at $\lesssim 0$ held-out
$R^2$): consistent with a large unmeasured slow-state component, these are the
quiet/latent regime rather than task-driving blocks. The minimal set that
exercises all three axes --- heading, turning, forward --- is therefore
\{Rotation, OMR~Turning, Gain\}, with OMR as the anchor.

\paragraph{Caveats and the engineering lift.}
The main work is heterogeneous stimulus encoding: each block has a different
input (rotation velocity, grating coherence, luminance, L/R light, closed-loop
gain), so a unified model needs a common input vector spanning all of them, not
just the heading drive $s(t)$ --- and the continuous drive is currently
reconstructed only for Rotation (and, coarsely, Turning); the other blocks
would have to be lifted from the stimulus log into proper model inputs. Two
blocks are closed-loop (Gain, Position), where the visual input is not cleanly
exogenous because it depends on the fish's own swimming; Random-Dots,
Position, Open-Loop and Darkness carry little ring-pool signal and serve mainly
as the quiet/baseline regime (and, for Darkness, as the cleanest test of
whether the circuit drifts on its own --- the unstructured, route-1 limit of
the same idea). We leave the unified multi-task model to future work; the
present section establishes that the data to constrain it --- nine conditions,
the same neurons throughout, an independent behavioural covariate --- are
already in hand.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_zebrafish_block_encoding.png}
  \caption{\textbf{Per-block encoding diagnostic: where the recurrent pool is driven,
  and where forward-swim matters.} Within each block, the recorded ring-pool
  $\Delta F/F$ is regressed on the signals available everywhere --- the
  stimulus-condition code (one-hot) and the forward/turning swim (lagged) ---
  with blocked-CV ridge and held-out $R^2$ (the encoding complement of the
  decoding analysis in \cref{fig:zhd_obs_control}).
  \textbf{(a)} Held-out $R^2$ of the recurrent pool, stimulus-only (grey) vs.\ the
  full stimulus$+$swim model (blue). The activity is most explainable in
  \textsc{turning}, then \textsc{flash}, \textsc{rotation},
  \textsc{taxis}; \textsc{gain}, \textsc{position}, \textsc{open~loop} and
  \textsc{dark} are at or below zero (the ring activity peak is not linearly driven there).
  \textbf{(b)} Unique forward-swim variance $\Delta R^2(\text{forward}\mid
  \text{stim})$ per block, against a circular-shift surrogate null
  ($\pm 2$\,SD); $\ast$ marks blocks where forward beats the null
  ($p<0.05$) --- \textsc{rotation}, \textsc{flash}, \textsc{taxis},
  \textsc{turning}. Forward-swim therefore carries real, temporally-aligned
  ring-pool variance beyond the stimulus across several conditions, the
  encoding-side evidence for a behaviour-linked second variable.}
  \label{fig:zhd_blockenc}
\end{figure}

\section{Outlook: which circuit properties are necessary, sufficient,
and biologically meaningful?}
\label{sec:outlook_future}

The results above establish that the connectome-constrained
circuit integrates heading and translation on the nominal $917$-cell
connectome, learns under multiple behavioural target families, and ---
on the $839$-cell precursor --- produces calcium activity that
visibly matches the recorded ZAPBench data when the observation loss
is on. They leave open the harder question of \emph{what about this
particular network} is actually doing the work. Six directions
follow directly from the experiments already in this paper and
together would convert the present existence proof into a structural
account of the computation.

\paragraph{Is the connectome necessary?} Re-training the same task
family on random networks matched for size, density, and E/I ratio
--- and on systematically rewired versions of the measured
connectome --- isolates how much of the observed performance comes
from the wiring as opposed to the network's capacity. The expected
gradient (intact $\to$ degree-preserved rewiring $\to$
Erd\H{o}s--R\'enyi at matched density) directly speaks to whether
the larval brain's connectivity is computationally efficient or
merely sufficient.

\paragraph{What E/I ratio supports joint heading + translation?}
The recurrent pool is predominantly inhibitory, and the v1 vs v2
connectome variants flip the IPN12 sign while keeping every other
edge fixed. Sweeping a softer version of that switch --- a controlled
inhibitory fraction at constant density --- and re-fitting the joint
swim-integration task would identify the E/I window inside which
both heading and forward distance remain decodable, and how close
the measured ratio sits to its centre.

\paragraph{Minimum connectivity for path integration.} An edge-pruning
sweep against the trained both / position-2D models, scoring the
task error as edges are removed in order of magnitude, gives the
sparsity floor below which integration fails. The same sweep
distinguishes a smoothly degrading regime (graceful) from a
brittle one (cliff at a critical sparsity), and identifies the
small core of edges that carries the computation.

\paragraph{Specialised or mixed-selectivity coding?} The
angular/displacement partition of \cref{sec:results_four_classes} is
a coarse readout of how the trained recurrent pool divides the work.
A finer per-neuron mutual-information analysis on $\hat\theta$,
$\hat d$, and their interaction would say whether the circuit
converges on disjoint heading and distance subpopulations or relies
on neurons that jointly encode both --- and whether mixed selectivity
emerges only when the task complexity demands it.

\paragraph{How much does calcium supervision reduce solution
degeneracy?} The observation loss already shifts each model toward a
small neighbourhood of recordings-consistent solutions
(\cref{sec:results_obs_sweep}). Training $K$ independent seeds with
$\lambda_{\mathrm{obs}} = 0$ and again with $\lambda_{\mathrm{obs}}
> 0$ and comparing the spread of $W^{\mathrm{rec}}$, $W^{\mathrm{in}}$,
$W^{\mathrm{out}}$, and the resulting per-cell tuning would quantify
how much identifiability the observation loss adds beyond the
connectome support and the task loss alone.

\paragraph{Exteroceptive vs proprioceptive translation.} The
proprioceptive variant (\cref{method:proprioception}) routes
$v_{\mathrm{fwd}}$ through motor-efferent cells in parallel with the
exteroceptive (pt-IPN1) pathway. Ablating each pathway in turn at
test time --- on the same trained circuit --- decomposes the
translation drive into the two physiologically distinct sources and
predicts which calcium signatures should disappear under selective
silencing of either limb, providing a directly testable bridge to
in-vivo perturbation experiments.

\appendix

\section{ZAPBench training data}\label{method:zapbench}
The recorded activity is the publicly released ZAPBench whole-brain
light-sheet calcium recording of a larval zebrafish, registered to the
same \emph{fish2} EM volume as the connectome (\cref{sec:anatomy}): the
$\Delta F/F$ trace of every segmented cell is taken from the ZAPBench
release and each connectome \emph{bodyId} is matched to its functional
ROI through the EM\,$\leftrightarrow$\,functional registration. Of the
$839$ neurons in the IPN12 circuit, $481$ carry a matched recording ---
$300$ ring-pool cells (\emph{IPNd}/\emph{IPNds}/\emph{IPN12}) and $181$
afferents (\emph{RIPN}, \emph{pt-IPN}). \Cref{fig:zhd_allblocks} lays out
the whole $\sim\!2\,\mathrm{h}$ recording: the nine task blocks, the fish's
own swim behaviour, the recorded activity of the three velocity-gate
afferent classes (ARTR, pt-IPN1, motor\_efferent), and the ring pool ---
the same $481$ matched neurons used throughout. The observation loss is
taken over all $481$ matched cells; the real-vs-model kinographs display only
the $300$ ring-pool subset. (These matched-cell counts are for the
$839$-cell \emph{IPN12 precursor} on which the calcium observation-loss
experiments of \cref{sec:results_obs_sweep} were run --- the circuit
\emph{before} the IPN-core families were added to form the nominal
$917$-cell circuit of \cref{sec:anatomy}. The task-only calcium baseline
of \cref{fig:zhd_obs_baseline} has since been re-run on the full
$917$-cell circuit, where $279$ ring-pool cells carry a matched
recording; the task-integration results of \cref{sec:results} all use
the $917$-cell circuit.) We supervise on the ``Rotations'' block, in which a
$45^\circ/\mathrm{s}$ drifting grating rotates the animal's virtual
heading: the true heading is read from the recorded stimulus channel and
differentiated to the angular-velocity drive $s(t)$, so the \emph{model}
is driven by the \emph{same} stimulus that produced the recording. The
block spans ${\sim}600\,\mathrm{s}$ ($656$ imaging frames at the
$1.09\,\mathrm{Hz}$, $0.915\,\mathrm{s}$ imaging rate). Because the model
runs at $\Delta t = 0.01\,\mathrm{s}$, the recorded $\Delta F/F$ is
linearly interpolated up to the model grid (edge-padded), and each neuron
is $z$-scored \emph{globally} over the whole block --- not per window ---
so the target keeps a single, trial-independent scale. The block is then
sliced into $1000$-frame ($10\,\mathrm{s}$) trials drawn from disjoint
train/test time spans, in the same format as the synthetic
swim-integration trials: channel~0 is the recorded drive $s(t)$ and the
first frame carries the $(\cos\theta_0,\sin\theta_0)$ heading cue, with no
warm-up. During training each task minibatch is augmented with $B_c$ of
these calcium trials in a single forward pass; the heading MSE of
\cref{eq:zhd_loss} is taken over the combined batch (the rotation trials
also carry a true heading), while $\mathcal{L}_{\mathrm{obs}}$ is
evaluated on the calcium trials only.

\begin{figure}[p]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_zebrafish_all_blocks.png}
  \caption{\textbf{The full ZAPBench session: nine conditions, one circuit.}
  All locally-available drives and behaviour over the whole recording
  ($7870$ frames $\approx 2\,\mathrm{h}$; the same $481$ matched neurons used
  throughout), with the nine task blocks separated and labelled
  (\textsc{gain, dots, flash, taxis, turning, position, open loop, rotation,
  dark}). \textbf{Top:} the per-frame stimulus-condition code. \textbf{Next
  two:} the fish's own swim behaviour --- \emph{forward} (bilateral tail-EMG
  envelope) and \emph{turning} (L/R asymmetry) --- available continuously in
  every block. \textbf{Middle three:} the recorded $\Delta F/F$ of the three
  velocity-gate afferent classes, in the \cref{fig:zhd_3d} colour code ---
  \emph{ARTR} (blue, angular drive), \emph{pt-IPN1} (orange, exteroceptive
  forward) and \emph{motor\_efferent} (green, angular efference);
  matched cells only, $z$-scored per neuron. \textbf{Bottom:} the recorded
  ring-pool $\Delta F/F$ ($n{=}300$, rastermap order, $z$-scored; same row set
  as \cref{fig:zhd_obs_reconstruction,fig:zhd_unmodeled}). The ring activity
  changes character block to block --- richly structured in Turning, Rotation
  and Flash, quiet in Darkness --- so the blocks excite different axes of the
  latent code and their union constrains a far richer circuit than Rotation
  alone. We have so far trained only on the Rotation slice.}
  \label{fig:zhd_allblocks}
\end{figure}

\section{Cell-type vocabulary}
\label{app:celltype_vocabulary}

\Cref{tab:zhd_mapping} lists the cell-type families of the HD circuit
exposed by the \emph{fish2} neuprint server, together with their roles
and \emph{fish2} type prefixes, and is referenced throughout the main
text whenever a cell-type tag is introduced.

\begin{table}[H]
\centering
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{l l l r l}
\toprule
Role & Zebrafish population & \emph{fish2} type prefix & $n$ & Sign / source \\
\midrule
Recurrent (HD ring) & dorsal IPN (\rpi) & \emph{IPNd*} & 313 & inh.\ (Dale); \rpi{} pop.\ of \citet{petrucco2023} \\
Recurrent (HD ring, subset) & dorsal-subset IPN & \emph{IPNds*} & 95 & inh.\ (Dale) \\
Recurrent (HD ring, IPN12) & dorsal IPN & \emph{IPN12\_a/\_b} & 108 & inh.\ (v1) / exc.\ (v2) \\
Recurrent (IPN-core) & core IPN & \emph{IPN28/29/31--36} & 184 & inh.\ (Dale) \\
Angular afferent (ARTR) & Rh2/3 $\to$ IPN & \emph{RIPN01/02/03\_a/03\_b} & 76 & exc.; ARTR \citep{dunn2016artr} \\
Motor efference & motor $\to$ IPN & \emph{RIPN11/12\_a/12\_c} & 65 & exc.; efference-copy (our assignment) \\
Other RIPN (residual) & habenula $\to$ IPN & \emph{RIPN05/12\_b} & 25 & exc.; not gate-routed \\
Exteroceptive translational afferent & pretectum $\to$ IPN & \emph{pt-IPN1} & 51 & exc.; optic / water-flow (our assignment) \\
\midrule
\multicolumn{4}{l}{\emph{total}} & 917 \\
\bottomrule
\end{tabular}%
}
\caption{HD-circuit cell-type families from the \emph{fish2}
reconstruction. \emph{Role}: how the cell is wired into the model
(recurrent ring, gate-routed afferent, or residual). \emph{Population}:
the corresponding zebrafish anatomical pool. \emph{fish2 type prefix}:
the \emph{fish2} connectome type name; the asterisk is a wildcard
grouping all sub-types with that prefix (e.g.\ \emph{IPNd13B},
\emph{IPNd17A}, \emph{IPNdp01} all match \emph{IPNd*}). $n$: cell
count in this pool. \emph{Sign / source}: Dale sign used in this work
(\emph{v1} and \emph{v2} are the two IPN12 sign variants), and the
functional identification or its origin --- a citation when the pool
matches a previously named population, ``our assignment'' when the
functional label is introduced here. \emph{IPN12*} (dorsal-IPN
recurrent) and the unrelated \emph{RIPN12*} (habenular afferent) are
distinct families despite the shared number. The \emph{IPN-core}
families (\emph{IPN28/29/31--36}) complete the recurrent GABAergic
substrate; the eight rows sum to the nominal $917$-cell circuit (the
$839$-cell precursor used for the observation-loss runs of
\cref{sec:results_obs_sweep} omits IPN-core).}
\label{tab:zhd_mapping}
\end{table}

\subsection{ARTR: opponent L/R gate and the origin of the oscillation}
\label{sec:results_artr}

The angular afferent pool (ARTR $=$ \emph{RIPN01/02/03\_a/03\_b};
\cref{tab:zhd_mapping}) is the anterior rhombencephalic turning region of
\citet{dunn2016artr}: a slow ($\sim$10--20\,s) antiphase oscillation between
the left and right clusters that biases successive swim bouts.

\paragraph{Sign-locked opponent gate.}
\emph{Hypothesis:} the recorded ARTR L/R anti-correlation needs an opponent
gate the heading task does not by itself impose. \emph{Test:} train the
angular gate unconstrained versus locked to opposite polarity
($v_{c,L}=-\softplus(\tilde v_{c,L})\le 0$,
$v_{c,R}=+\softplus(\tilde v_{c,R})\ge 0$, magnitudes free). \emph{Result:} the
unconstrained gate converges to a same-sign solution --- it integrates heading
equally well but drives both hemispheres in phase; the lock restores opponency
at no cost to task accuracy. Non-directional channels (forward-swim vigour,
luminance) carry no lock.

\paragraph{Two timescales.}
\emph{Hypothesis:} ARTR carries a slow stimulus-locked rhythm plus a faster
component of unknown origin (inherited vs intrinsic). \emph{Test:} band-split
the L/R means at a $16$\,s period and compare L/R coherence
(\cref{fig:zhd_artr_timescales}). \emph{Result:} over the whole rotation block the recorded ARTR L/R
population means are antiphase ($r_{LR}=-0.55$) and the sign-locked model
is more sharply so ($r_{LR}=-0.93$). Band-splitting separates two
components. The slow band ($>16$\,s,
$\sim$60\,s) is the turn-block --- antiphase and $\omega$-locked in both data
($r_{LR}=-0.57$) and model ($-0.96$), hence inherited. The fast band
($<16$\,s, $\sim$8\,s) is the heading-bump carrier ($360/|\omega|$) in the
model and is L/R phase-locked ($|r_{LR}|=0.90$); in the recording it is
incoherent ($r_{LR}=+0.03$) and left-lateralised ($7$\,L vs $1$--$2$\,R cells)
where the model is bilateral. An inherited
carrier is necessarily L/R-locked, so the recorded fast band is a graded mix
--- inherited carrier plus a cell-specific component at or below the
imaging-SNR floor.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.8\textwidth]{\figdir/fig_zebrafish_artr_kinograph.png}
  \caption{\textbf{Recorded versus modelled ARTR.} The recorded ARTR
  neurons matched to their counterparts in the sign-constrained $917$-cell
  model (driven by the recorded $\omega$), grouped by hemisphere (L block below
  the white divider, R above; original ZAPBench order). \textbf{(a)}~Angular
  drive $\omega(t)$: a square-wave rotation command.
  \textbf{(b)}~Recorded ARTR $\Delta F/F$ (per-row $z$-scored), L and R
  hemispheres.
  \textbf{(c)}~Model (sign-locked opponent gate), GCaMP-convolved, same neuron
  order as \textbf{b}.
  \textbf{(d)}~Recorded L/R population mean (ARTR$_L$ red, ARTR$_R$ blue; line
  $=$ mean, shading $=\pm$SD across cells).
  \textbf{(e)}~Its slow band ($>16$\,s, the turn-block).
  \textbf{(f)}~Its fast band ($<16$\,s, the bump-carrier band).
  \textbf{(g, h)}~The learned model traces for each ARTR cell
  ($z$-scored), ARTR$_L$ (\textbf{g}) and ARTR$_R$ (\textbf{h}),
  with the population mean in black.}
  \label{fig:zhd_artr_timescales}
\end{figure}

\paragraph{Decoder dependence of the modelled ARTR.}
Driving the $64$-bin-decoder model (\cref{sec:results_decoder}) with the
same recorded $\omega$ gives the companion kinograph of
\cref{fig:zhd_artr_timescales_kbins}. The modelled ARTR is again
antiphase and $\omega$-locked --- the recorded structure is reproduced
under either readout --- but the L/R opponency is somewhat weaker than
with the $(\cos\theta,\sin\theta)$ readout (whole-trace $r_{LR}=-0.59$
vs.\ $-0.93$; slow band $-0.94$ vs.\ $-0.96$). The decoder therefore does
leave a measurable imprint on the recurrent representation, even though
the qualitative inherited-oscillation picture
(\cref{fig:zhd_artr_oscillation_kbins}) is unchanged.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.8\textwidth]{\figdir/fig_zebrafish_artr_kinograph_kbins.png}
  \caption{\textbf{Recorded versus modelled ARTR under the $64$-bin
  decoder} (companion of \cref{fig:zhd_artr_timescales}). Same recorded
  ARTR neurons and panel layout as \cref{fig:zhd_artr_timescales}, with
  the model driven by the recorded $\omega$; here the recurrent circuit
  uses the $64$-bin softmax heading decoder (\cref{sec:results_decoder})
  rather than the $(\cos\theta,\sin\theta)$ readout. \textbf{(a)}~Angular
  drive $\omega(t)$. \textbf{(b)}~Recorded ARTR $\Delta F/F$, L and R
  hemispheres. \textbf{(c)}~Model (GCaMP-convolved, same neuron order as
  \textbf{b}). \textbf{(d)}~Recorded L/R population mean.
  \textbf{(e)}~Its slow band ($>16$\,s). \textbf{(f)}~Its fast band
  ($<16$\,s). \textbf{(g, h)}~The learned model traces for each ARTR
  cell, ARTR$_L$ (\textbf{g}) and ARTR$_R$ (\textbf{h}), with the
  population mean in black.}
  \label{fig:zhd_artr_timescales_kbins}
\end{figure}

\paragraph{Origin: inherited, not intrinsic.}
\emph{Hypothesis:} an intrinsic oscillator has a period set by its own time
constants --- invariant to $\omega$, sustained, and persistent without input;
an inherited one tracks the drive and stops with it. \emph{Test:} a
constant-$\omega$ sweep on the model (\cref{fig:zhd_artr_oscillation}; the
zero-input free-run is deferred to the darkness comparison below).
\emph{Result:} under constant $\omega$ the
ARTR oscillation period tracks the bump-rotation period $360/|\omega|$
(\textbf{a}) while its amplitude stays roughly constant across the swept range
(\textbf{b}) --- the signature of a carrier slaved to the rotating heading bump
rather than a clock with its own frequency. The model
oscillation is inherited, not intrinsic. This is structural: an intrinsic
rhythm needs L$\leftrightarrow$R inhibition (bistability) and a slow adaptation
variable (to set the $\sim$10--20\,s period), whereas our ARTR is
all-excitatory with $\tau=0.1$\,s and would latch rather than oscillate.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.7\textwidth]{\figdir/fig_zebrafish_artr_oscillation.png}
  \caption{\textbf{ARTR oscillation diagnostic}
  (sign-constrained $917$-cell model, recorded $\omega$).
  \textbf{(a)}~Dominant ARTR oscillation period $T_{\mathrm{ARTR}}$ (s)
  versus constant $\omega$ (purple), against the bump-rotation period
  $360/|\omega|$\,s (dashed).
  \textbf{(b)}~ARTR oscillation amplitude (std of the population mean) versus
  constant $\omega$.}
  \label{fig:zhd_artr_oscillation}
\end{figure}

\paragraph{The oscillation is a property of the circuit, not the decoder.}
Repeating the constant-$\omega$ sweep on the $64$-bin-decoder model
(\cref{sec:results_decoder}) gives the same diagnostic
(\cref{fig:zhd_artr_oscillation_kbins}): the ARTR period tracks the
bump-rotation period $360/|\omega|$ and the zero-input free-run decays to
a fixed point. Swapping the $(\cos\theta,\sin\theta)$ readout for a bin
readout therefore leaves the inherited (not intrinsic) character of the
ARTR oscillation unchanged --- consistent with the oscillation being a
property of the recurrent circuit rather than of the heading decoder.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.7\textwidth]{\figdir/fig_zebrafish_artr_oscillation_kbins.png}
  \caption{\textbf{ARTR oscillation diagnostic under the $64$-bin decoder}
  (companion of \cref{fig:zhd_artr_oscillation}; sign-constrained
  $917$-cell model, recorded $\omega$).
  \textbf{(a)}~Dominant ARTR oscillation period versus constant $\omega$
  (purple), against the bump-rotation period $360/|\omega|$ (dashed).
  \textbf{(b)}~ARTR oscillation amplitude (std of the population mean)
  versus constant $\omega$.}
  \label{fig:zhd_artr_oscillation_kbins}
\end{figure}

\paragraph{Darkness: a test for the missing oscillator.}
\emph{Hypothesis:} if the recorded ARTR is intrinsic it keeps oscillating once
the command is removed; the command-driven model does not. \emph{Test:} the
ZAPBench session runs a rotation block straight into a darkness block in the
same fish; drive the model identically (recorded rotation $\omega$, then zero
input) and compare (\cref{fig:zhd_artr_dark}). \emph{Result:} in the dark the
recorded ARTR keeps alternating ($|L-R|$ std $=0.72$) while the model is
near-flat ($0.14$, $\sim$5$\times$ smaller). The cleanest version of the
same test is a zero-input free-run from a small perturbation, with no
preceding drive at all: the model decays to a fixed point (population-mean
amplitude $\approx 0.003$), confirming it carries no self-sustained
oscillation. The recording carries an intrinsic dark-state rhythm
(cf.\ \citealp{dunn2016artr}) the model largely lacks; its much-reduced dark
response is expected by construction, so this is a prediction--data gap that names the
missing ingredients (recurrent inhibition $+$ slow adaptation) --- a
falsifiable target for future work.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{\figdir/fig_zebrafish_artr_dark.png}
  \caption{\textbf{ARTR at the rotation$\to$dark transition.} The continuous
  ZAPBench \emph{open loop} $\to$ \emph{rotation} $\to$ \emph{dark} blocks; the
  model is driven with the recorded rotation $\omega$ followed by zero input
  (dark block, shaded). \textbf{(a)}~Angular drive $\omega(t)$: zero in open
  loop, the $\pm30^\circ/$s square wave in rotation, zero in dark.
  \textbf{(b)}~Recorded ARTR L/R population mean (ARTR$_L$ red, ARTR$_R$ blue),
  normalised. \textbf{(c)}~Model ARTR L/R population mean, normalised.
  \textbf{(d, e)}~Recorded and modelled ARTR
  kinographs (per-row $z$-scored, L block below the white divider; dashed lines
  mark the rotation and dark onsets).}
  \label{fig:zhd_artr_dark}
\end{figure}

\bibliographystyle{unsrtnat}
\begin{thebibliography}{9}

\bibitem[Petrucco et al.(2023)]{petrucco2023}
L.~Petrucco, H.~Lavian, Y.~K.~Wu, F.~Svara, V.~\v{S}tih, R.~Portugues.
Neural dynamics and architecture of the heading direction circuit in
zebrafish. \emph{Nature Neuroscience} \textbf{26}, 765--773 (2023).

\bibitem[Allier et~al.(2026)]{cx2026}
C.~Allier et~al.
Central complex: identifiable computation, non-identifiable
implementation. Companion manuscript, in preparation (2026).

\bibitem[Dana et al.(2019)]{dana2019}
H.~Dana, Y.~Sun, B.~Mohar, et~al.
High-performance calcium sensors for imaging activity in neuronal
populations and microcompartments. \emph{Nature Methods} \textbf{16},
649--657 (2019).

\bibitem[Feierstein et al.(2023)]{feierstein2023}
C.~E.~Feierstein, M.~H.~M.~de~Goeij, A.~D.~Ostrovsky, A.~Laborde,
R.~Portugues, M.~B.~Orger, C.~K.~Machens.
Dimensionality reduction reveals separate translation and rotation
populations in the zebrafish hindbrain. \emph{Current Biology}
\textbf{33}, 3911--3925 (2023).

\bibitem[Dunn et al.(2016)]{dunn2016artr}
T.~W.~Dunn, Y.~Mu, S.~Narayan, O.~Randlett, E.~A.~Naumann, C.~T.~Yang,
A.~F.~Schier, J.~Freeman, F.~Engert, M.~B.~Ahrens.
Brain-wide mapping of neural activity controlling zebrafish exploratory
locomotion. \emph{eLife} \textbf{5}, e12741 (2016).

\bibitem[Khona \& Fiete(2022)]{khona2022}
M.~Khona, I.~R.~Fiete.
Attractor and integrator networks in the brain.
\emph{Nature Reviews Neuroscience} \textbf{23}, 744--766 (2022).

\bibitem[Seung(1996)]{seung1996}
H.~S.~Seung.
How the brain keeps the eyes still.
\emph{Proc.\ Natl.\ Acad.\ Sci.\ USA} \textbf{93}, 13339--13344 (1996).

\bibitem[Major et al.(2004)]{major2004}
G.~Major, R.~Baker, E.~Aksay, B.~Mensh, H.~S.~Seung, D.~W.~Tank.
Plasticity and tuning by visual feedback of the stability of a neural
integrator. \emph{Proc.\ Natl.\ Acad.\ Sci.\ USA} \textbf{101},
7739--7744 (2004).

\bibitem[Gon\c{c}alves et al.(2014)]{goncalves2014}
P.~J.~Gon\c{c}alves, A.~B.~Arrenberg, B.~Hablitzel, H.~Baier,
C.~K.~Machens.
Optogenetic perturbations reveal the dynamics of an oculomotor
integrator. \emph{Frontiers in Neural Circuits} \textbf{8}, 10 (2014).

\bibitem[Kim et al.(2017)]{kim2017}
S.~S.~Kim, H.~Rouault, S.~Druckmann, V.~Jayaraman.
Ring attractor dynamics in the \emph{Drosophila} central brain.
\emph{Science} \textbf{356}, 849--853 (2017).

\bibitem[Chaudhuri et al.(2019)]{chaudhuri2019}
R.~Chaudhuri, B.~Ger\c{c}ek, B.~Pandey, A.~Peyrache, I.~Fiete.
The intrinsic attractor manifold and population dynamics of a canonical
cognitive circuit across waking and sleep. \emph{Nature Neuroscience}
\textbf{22}, 1512--1520 (2019).

\bibitem[Lyu et al.(2022)]{lyu2022}
C.~Lyu, L.~F.~Abbott, G.~Maimon.
Building an allocentric travelling direction signal via vector
computation. \emph{Nature} \textbf{601}, 92--97 (2022).

\bibitem[Yang et al.(2024)]{yang2024}
C.~Yang, L.~Mammen, B.~Kim, M.~Li, D.~N.~Robson, J.~M.~Li.
A population code for spatial representation in the zebrafish
telencephalon. \emph{Nature} \textbf{634}, 397--406 (2024).

\end{thebibliography}

\end{document}
