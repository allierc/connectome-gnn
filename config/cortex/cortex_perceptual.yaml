description: "Single-rule cortex smoke test (Yang dm1 only) — fastest debug path."
dataset: task_cortex_perceptual
simulation:
  params:
    - - 1.0
graph_model:
  aggr_type: add
plotting: {}
training: {}
task:
  task_type: cortex
  task_only: true
  cortex:
    rules: ["dm1"]
    rule_weights: []
    ruleset: "all"
    n_trials_train: 8
    n_trials_test: 4
    n_steps_max: 400
    seed: 0
    hp_overrides: {}
